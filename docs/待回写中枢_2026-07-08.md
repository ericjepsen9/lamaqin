# 待回写规划中枢清单(2026-07-08)

> 性质:**App 线施工产出**,待 PM 同步回中枢 `sss/Planning/` 对应文件,再按**文档分发方案A 单向覆盖**回本 repo 的 `docs/` + `CLAUDE.md`(防分叉)。
> 接力协议(CLAUDE.md 规定):改中枢共享文档**前**先读 `决策定稿_课程专业数据模型` 落地段;改**后**在该文件追加一行(日期 + 改了啥)。
> 三类分开放:**A 工程规约→中枢 `CLAUDE.md`** / **B 产品决策/口径→决策文档** / **C schema 订正→`schema_phase1`**。
> 来源:2026-07-08 一致性检查(commit 84e3a5c/d0c0c58)+ 8-agent 全景审计。全部为"决策已定、源头文档未同步"的漂移条,非新决策。

---

## A. 工程/施工规约(中枢 `CLAUDE.md`)

### A1. 新增「总则·三易」(已就地加入本 repo `CLAUDE.md` §8 顶部,待回中枢同源)
> 草稿(可直接粘贴到中枢 CLAUDE.md 施工规约节顶部):
```
> 总则·三易(一切施工遵循;冲突时报 PM 权衡):每次落地先问「是否更易维护/易扩展/易迁移」。
> - 易维护:单一真源(同一逻辑只一处)、魔法数集中并标「占位·待核」、不留孤儿代码(写了就接入)、改动挂判例/决策出处。
> - 易扩展:优先数据驱动(加内容=加数据行,勿加 if/else 或 in-table CHECK 硬编码枚举);判定形状(维度/周期/阈值)尽量配置化。
> - 易迁移:核心业务逻辑放 DB/可移植层;平台专属(pgvector/pg_cron/storage/Edge)隔离成独立迁移;时区禁 CURRENT_DATE;前端一律经 lib/queries 查询层。
```

### A2. §8.3「类型生成」措辞与现实脱节(订正)
- **现状**:§8.3 写「`database.ts` 由 `scripts/gen-types-from-migrations.cjs` 生成、勿手改、生成器漏就补生成器」。
- **实况(2026-07-08 验证)**:真实 `database.ts`(3414 行·tsc 0 错)是用**官方 `supabase gen types typescript --project-id ubyzyadlzmtgxvbxanbr`(需连库)**产的精确版;本地 `.cjs` 是"近似"降级方案,已滞后——无库环境重跑本地 `.cjs` 会 **-269 行 / +125 tsc 错**(丢 programs.display_order 等列与 Insert 类型)。
- **应改为**:§8.3 讲清「**官方 gen types 为准**(有库时跑,自然含新函数)、`.cjs` 为无库降级、无库时勿整份重生成只可按现有风格手补缺失条目」。并排一次维护:下次连库跑官方 gen types 覆盖,消除 d0c0c58 手加的 get_care_dims/get_cohort_care_dims 两条手工类型。

---

## B. 产品决策/口径(中枢决策文档)

### B1. 状态机档位:4级 → 3档(DEF-1,决策已定·文档未同步)
- **现状**:`requirements_master §165` 仍写「4 级状态机(on_track/slightly_behind/falling_behind/at_risk)」;生产迁移 `20260618000060_practice.sql` 的 `current_status` CHECK 仍冻历史 7 态。
- **应改为**:按 DEF-1 收敛为 **3 档**(on_track / falling_behind / at_risk,删 slightly_behind 等)。req_master §165 改 3 档措辞;~~`current_status` CHECK 待专门迁移收到 3 态(排期)~~ **迁移已写好(20260708000200,含存量归并 UPDATE),但随 07-04 批次整体未应用到 dev(2026-07-09 审计)——已入待应用 SQL 包**。另:database.ts 的 current_status 类型仍是旧 7 值联合(2891/2930/2969 行),迁移应用后需随类型重生成一并收敛。
- 出处:`docs/requirements_master_2026-05-31.md:165` vs 实现 `20260706000100_vow_status_fn.sql`(末尾 COMMENT「3档+na」)、`tests/casebook/state-machine.md` DEF-1。

### B2. 碎片池表述废止(DR-91,决策已定·文档未同步)
- **现状**:`feature_inventory_v2.md:144` 仍写「碎片池凑满30分=1座、余数清零、碎片累计对师兄可见」。
- **应改为**:按 DR-91(放弃短座合并、无碎片池)**废止该句**,改为「不满30分各记0座、不跨记录凑碎片」;并向教务报备此为对大纲(行110)的有意偏离。
- 出处:`docs/feature_inventory_v2.md:144` vs `tests/casebook/counting.md:342-343`。

### B3. 藏历/殊胜日时区口径统一为 UTC+8 固定(决策075,文档三处打架) — ✅ 2026-07-15 App repo 侧已订正
- **现状(订正前)**:`schema §16.1b` 藏历行写基准=「用户手机本地时区」;`CLAUDE.md §1` 时区四层亦写「藏历殊胜日按用户手机本地」;而**决策075 裁定「藏历殊胜日=UTC+8 固定」**。三处不一致。
- **已改为**:统一为**藏历=UTC+8 固定**(全球统一按 UTC+8 算"今天",与手机本地无关;双时区显示留 v1.5+)。已同步订正 `CLAUDE.md §1`、`schema_phase1 §16.1b`、`schema_phase1` 场景12 段落(177-181行)、`cohorts.timezone` 字段注释(582行)、16.11决定表 #95(2895行)——均改为 UTC+8 固定口径,代码本身(`lib/tibetan.ts::todayUTC8()`)此前已正确实现,无需改代码。
- **仍待办(PM 侧,非本 repo 范围)**:按文档分发方案A,这些订正本应先落中枢 `sss/Planning/` 对应文件,再单向覆盖回本 repo 防分叉。本次是本 repo 侧直接订正(内容与决策075一致,不会冲突),PM 有空时仍需把同样的措辞同步进中枢源文件,避免下次"中枢→App repo"覆盖时被旧措辞打回。
- 出处:`docs/schema_phase1_2026-05-31.md:2760`、`CLAUDE.md §1` vs `design_v2_decisions.md`(决策075)、`feature_inventory_v2.md:202`。

### B4.「圆满」口径**其实已定**(2026-07-12 订正,原B4"教务口径未定"是訛误)+ 圆满课次卡延后未登记
- ⚠️ **原记录有误,先订正**:本条目最初(07-08)写「何为圆满教务口径未定」,抄自 `get_cohort_week_totals` 迁移注释的原话——但那句注释本身就是訛误。`tests/casebook/report-aggregation.md` RA-2 早已指出:圆满规则**已经定了**(决策091+大纲行82-91:正式课=听≥1+看≥1+答思考题,限制性课=听+看),`lesson/[id].tsx` 的 `wensiDone&&answered` 现在就是照这个规则算的。**没定的只是"自动判定"这个工程动作**(prd 明确 v1.5+,v1.0 展示原始数据供人工判)——迁移注释与本条目均已订正(commit 见 App repo 记录)。
- **仍需登记进中枢的是**:圆满自动判定(v1.5+,规则已知、只差工程实现)这条延后本身,此前**只存在于迁移注释里、未进任何中枢待决/延后清单**——延后本身是真的,但原因不是"口径未定"。
- **应改为**:中枢延后清单补一条"闻思圆满自动判定"(规则=决策091/大纲行82-91,工程延后v1.5+,与延后-4应为同一条——若延后-4已覆盖此项则本条只需核对措辞一致,不必重开新号)。
- 出处:`supabase/migrations/20260701000010_get_cohort_week_totals.sql` 注释(已订正)、`tests/casebook/report-aggregation.md` RA-2、`docs/design_v2_decisions.md` 决策091/延后-4。

### B5. 起修豁免口径同源(prd §5.9.4 旧口径 vs 代码 SM-4)—— ✅ 2026-07-14 已核对完成
- **现状(已核实)**:`prd §5.9.4`(`docs/prd_2026-05-31.md:953-955`)原文只有一句"起修后 7 天内,任何愿强制 on_track"——这部分跟 SM-4 一致。但代码还实现了另一分支"起修日未到(`start_date>今天`)→ na(不判)",prd §5.9.4 完全没提这一句。
- **溯源**:na 分支不是代码凭空加的,来自更晚的决策081(`design_v2_decisions.md:4390-4416`,2026-06-13)→ 延后-27 → `tests/casebook/state-machine.md` DEF-5 正式裁定——只是 prd(05-31基线)停留在更早的版本,没回填这条后续裁决。
- **结论/应做**:代码是对的(照最新决策链实现),**该订正的是 prd**——`prd §5.9.4` 补上"起修日未到→na"这一句,来源标注决策081/延后-27/DEF-5。不是代码bug,不用改代码。

### B6b. 学经留级校验口径矛盾(PD-18·2026-07-08 裁决=admin 人工)
- **现状**:`requirements_master §16`/schema 写「次数上限 admin 人工掌握·v1.0 不强制」;但 `feature_inventory §2.10`/决策090 写「v1.0 系统校验」。两处矛盾。
- **裁决(PD-18)**:以 requirements_master/schema 为准(§0 层级更高)= admin 人工掌握、v1.0 不系统硬卡。
- **应改**:feature_inventory §2.10 与决策090 的「v1.0 系统校验」订正为「admin 人工掌握」。

### B6c. 师兄端状态色可见性(SD-5·2026-07-08 裁决=显示)
- **现状**:决策035/107「师兄端零呈现(无状态色/关怀)」;但三红线已撤(173),限制解除。
- **裁决(SD-5)**:师兄端**显示自己的状态色(含掉队提示)**(关怀名单仍管理端 only)。
- **应改**:决策035/107 就「师兄本人状态色」部分标注被 SD-5 覆盖;中枢记录师兄端可见性新口径。需后续做师兄端状态展示 UI。

### B6. 治理红线簇(撤173 后的文档分叉,建议一次性厘清)
- 撤173 口径:`CLAUDE.md §3`(已就地撤三红线)与中枢决策记录是否同源;`#193 互看=0` 是否真撤(两文档打架);03 验收报告 E4 已被决策118/113 推翻但未勘误;功德会条款(req §16 / prd §14.3)废除待同步。
- **应做**:PM 在中枢一次性厘清撤173 后哪些红线仍生效(状态色/排名/密法痕迹/互看),回写各文档,消除分叉。
- 出处:`tests/casebook/state-machine-care-audit.md:187`。

---

## C. schema 订正(中枢 `schema_phase1`,App 主写)

### C1. §13 药师七佛经名订正(缺「功德」二字)
- **现状**:`schema §13` 学经自选七经注释写《药师七佛经》,少「功德」。迁移 `20260707000100_optional_sutra_list.sql` 已按大纲(行497-499)把**数据**订正为《药师七佛功德经》,但 schema 注释本身未改。
- **应改为**:schema §13 注释订正为《药师七佛功德经》。(经名权威=大纲;属自选经 practice 名,非讲记课程,在 App schema 写权范围内。)
- 出处:`docs/schema_phase1_2026-05-31.md:2418` vs `20260707000100_optional_sutra_list.sql:5-7`。

### C2. 藏历迁移的跨库/公开视图收尾(运维协调)
- **现状**:`tibetan_calendar_final` 头注两条待办未闭环:①本文件需在 **sss-dev 跑一遍**使 dev 不漂;②`multiplier` 已入库但 PM 定不展示,**官网公开视图需剔除 multiplier**。
- **应做**:运维在 sss-dev 应用该迁移;官网线(另一 Claude/子项目)改公开视图剔除 multiplier。属跨库/运维,非 App 可自决。
- 出处:`supabase/migrations/20260619000000_tibetan_calendar_final.sql:15`。

### C3. schema_phase1 补两处已应用迁移未同步(2026-07-12 一致性检查发现)
- **现状**:`practice_logs` 表的 `client_token`(迁移 `20260712000000_practice_logs_idempotency.sql`,弱网幂等去重用)与全新 `notifications` 表(迁移 `20260711000000_notifications.sql`,站内通知)均**已应用**(`check_schema_parity.py` 已确认线上无漂移),但 `docs/schema_phase1_2026-05-31.md` 全文对二者**零匹配**(含 `practice_logs` 建表段 1550-1583 行未见 `client_token`)。
- **应改为**:schema_phase1 补 `practice_logs.client_token`(text,partial unique index)与 `notifications` 表(结构见迁移文件头注)两段。
- 出处:`docs/schema_phase1_2026-05-31.md`(全文搜索 `client_token`/`notifications` 均 0 命中) vs `supabase/migrations/20260711000000_notifications.sql`、`20260712000000_practice_logs_idempotency.sql`。

---

## D. 发布节奏决策(2026-07-10 新记——这条是**真决策**,不是漂移条,需 PM 在中枢正式登记编号)

### D1. v1.0 首发范围切分
- **决策**:v1.0 首发提审版 =「4 条 Apple 硬性门槛」(账号注销真实接线、隐私政策/服务条款定稿、Apple 开发者账号确认、提审前人工真机走一遍核心流程) +「5 条强烈建议同批」(useCurrentUser 静默失败修复、关怀详情页误判修复、held_back_count 静默重置修复、11 表 profiles 外键歧义排查、生产 SMTP 状态确认)。其余(11 项决策性延后功能、iOS Maestro 自动化建设、大部分测试基建投入)明确放 v1.1 及以后,**不因求全拖上线**。
- **依据**:2026-07-10 上线就绪度审计 + 打包发布计划(App Claude 产出,PM 讨论后拍板"按建议做")。
- **已落地**(工程侧,本决策范围内的5条强烈建议项,2026-07-10 完成):useCurrentUser(`lib/queries/profile.ts`)、关怀详情页+关怀清单列表(`lib/queries/care.ts`+`app/(admin)/care/[studentId].tsx`)、held_back_count(`lib/mutations/classes.ts`)三处真实修复;11 表 profiles 外键歧义排查完成,真实暴露的是 class_members(4处)+class_admins(1处),已修复验证,其余 10 张表确认零代码触发。commit `d9a404b`。
- **仍待**(非工程,需 PM/法务/运营推进):账号注销口径定案、隐私文本定稿、Apple 账号状态确认、SMTP 状态确认、提审前人工真机检查排期。
- 出处:本次对话(App Claude 会话)+ 发布计划文档(未入 repo,是聊天内 Artifact)。**这条决策编号需 PM 回中枢按序分配**,App 侧不掌握编号计数器,不自行编号。

### D2. 离线内容支持范围(2026-07-12 新记——真决策,收窄决策089「不做离线缓存」条款,需 PM 在中枢正式登记编号)
- **背景核实**(2026-07-12 一致性检查,读 `design_v2_decisions.md` 原文核对):**决策089**(2026-06-14·10.2 弱网处理)原文="v1.0 = 网络错误友好提示 + 重试;**不做乐观UI、不做离线缓存**;乐观UI推v1.5+;**完整离线推v2.0+**;音频下载离线v1.0做(另算,决策080/3.13)"。**决策080**(2026-06-13·3.13 资料托管)原文="音频 Supabase Storage(在线听+**可下载离线**)/ 视频 YouTube 在线(不下载)";其校验行进一步显示音频离线原意是**大陆视频访问的既定 tradeoff**(YouTube 大陆不可达→音频离线保底),不是通用离线策略。`prd_2026-05-31.md:1249`(§5.14 v1.0 范围·纯在线)据此写死"❌ 不做离线缓存"。
- **决策**(PM 2026-07-12 对话中拍板):v1.0 **追加两层只读离线缓存**(仍不做"完整离线"=打卡/报数等**写操作**的本地读写同步,那部分维持决策089原判、仍推 v2.0+):
  1. **第一层**(浏览即生效,无需用户操作):课程/课时/自学/大学演讲等**文字/结构化内容**类查询结果持久化到本地(AsyncStorage),白名单见 `lib/query-persist-allowlist.ts`;进度/状态类查询(会随学习变化,离线陈旧值可能让本人误判自己进度)明确排除在外。
  2. **第二层**(用户主动点「离线下载」):**音频**与**观修课件逐页图片**显式下载缓存到本地磁盘(`expo-file-system`),不下载不缓存;「设置→离线下载管理」可查占用/删除。web 平台整体不支持(`expo-file-system` 新版 API 官方不支持 web),下载入口在 web 上不渲染。
  3. **明确仍不做**(维持范围外):①观修课件"无逐页图、只有旧 downloadUrl 外链"的兜底分支(仍 `Linking.openURL` 外部打开、需要网络)——该分支本是给尚未回填 `slide_image_urls` 的旧数据用的临时兜底,真正做离线需引入新的文档查看器依赖,且该数据缺口预期被 ETL 持续回填收窄,故不做;②YouTube 内嵌视频离线(决策080 从未打算下载视频,只离线兜底音频);③打卡/报数等写操作的离线读写同步(仍是决策089"完整离线"范畴,v2.0+)。
- **应改为**:`prd_2026-05-31.md:1249` "❌ 不做离线缓存" 一行需加限定语,改为类似"❌ 不做完整离线(打卡/报数等写操作本地读写同步);✅ 只读内容缓存(课程/课时文字 + 音频/观修课件下载)v1.0 做(决策0xx,收窄决策089)"。`feature_inventory_v2.md:200` 同款措辞一并订正。
- 出处:本次对话(App Claude 会话,2026-07-12)+ `docs/design_v2_decisions.md:4125-4151`(决策089)、`:4428-4457`(决策080)+ `docs/prd_2026-05-31.md:1247-1249`。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D3. 课程圆满/毕业升学条件的口径拍板(2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:App Claude 就"课程圆满"定义出选项(仅闻思/仅功课/两者都要)请 PM 拍板,并核实毕业/升学量化维度此前用的是运营占位阈值(出勤85%/70%、修量1.0/0.8倍近似、考试60/80分固定线),与教学大纲原文(`docs/juexue_v2_design/reference/预科19届学修大纲-原文提取.txt`)的真实规则不符。
- **决策**(PM 2026-07-12 对话中拍板):
  1. **课程圆满** = 闻思圆满(该课全部课时:听/看+读+答思考题达标,盲聋按次数≥2)**AND** 功课圆满(该专业配置的全部有限终点功课模板达标)——**两者都要**,不是二选一。
  2. **毕业/升学量化维度一律按大纲原文绝对数判断**,弃用比例型占位阈值:出勤只有绝对"93次"门槛(非百分比健康色);考试合格线视出勤是否达93次分级(≥93→30分;<93→开卷72/闭卷60);修量看模板 target_count 是否达标,大纲无"接近达标"这种比例概念。
  3. **考试**:v1.0 只做人工线下考+后台"录入成绩"(分数+开卷/闭卷两种形式),合格与否系统按上条规则自动算,不再人工勾选是否合格。明确不做(范围排除):60岁免考(无生日字段)、"达不到93次可改考2次每次30分"替代路径(需跨记录历史聚合)。
  4. **功课圆满自动判定**从"v1.5+人工看原始数据"(延后-4)提前到 v1.0 做成 DB 视图(实时查询算,不落存储列,躲开"Edge cron从未真正跑过"覆辙)。
- **已落地**(工程侧):`lib/exam-pass-line.ts`(合格线分级函数+单测)+ `exam_grades.exam_format` 列(`20260712000100_exam_format.sql`)+ `useRecordExamGrade` 重写(含"不计出勤"临时场次排除);`lib/admin-thresholds.ts` 删 `PRACTICE_TARGET`/`EXAM` 两个占位常量;4 张圆满视图 `v_lesson_completion`/`v_course_completion`/`v_program_practice_completion`/`v_program_practice_summary`(`20260712000200_completion_views.sql`,security_invoker)+ 本地 SQL harness 新增 `14_completion_views.sql`(13/13断言通过,全量343断言无回归);管理端「报数升学」列表/详情页修量色阶/考试展示改用新逻辑,详情页③修量新增按 `v_program_practice_completion` 的专业配置功课明细(含尚未发放模板,按0进度显示、不悄悄漏判)。
- **2026-07-12 追加落地**:`v_lesson_completion`/`v_course_completion` 已接 UI(PM 问"接UI时要不要顺便统一现有圆满判定",选"统一(推荐)")——course/[id].tsx 新增"本书圆满"标(`v_course_completion.is_complete`),同时 course/[id].tsx(`useMyCourseLessonStatus`)、class.tsx(`useLessonStatusMap`)、dossier.ts(`useMyStudyCompletion`)三处既有消费方全部改查新视图,`lib/queries/courses.ts` 原有的本地 `isLessonComplete()` 判定函数已删除(核对 `tests/casebook/counting.md` HQ-8 确认它对盲聋只判断"听/看过没有"的布尔值,比判例"听/看2遍"偏松,是判定偏差不是刻意设计,已随迁移 `20260712000300_lesson_completion_touched.sql` 一并修正)。
- **仍待**(2026-07-12 更新,两条均已解决,见下 D7/D8):~~"圆满课次"可扩展 `get_cohort_week_totals`(班级周报表汇总维度),本轮未做,是否要做待 PM 排期~~ **已按选项C完成,见 D8**;~~出勤 85%/70% 健康色阈值仍是「占位·待核」(大纲本身无此百分比健康色概念,是否保留、如何呈现待 PM 定)~~ **已选项B去色完成,见 D7**。
- **2026-07-12 独立审计**(专开一个 agent 反查,不采信我自己的原始推理):本轮引用的决策编号(091/延后-4/决策017/HQ-8/012/125/120)逐条核对 `design_v2_decisions.md`/`tests/casebook/counting.md` 原文,内容均对得上;唯一精度提醒——"决策091"是个包含7项的综合决策(3.1~3.12),闻思圆满判定具体是其中 **决策091.2** 这一项,引用可以更精确但内容本身没错。盲聋"听/看2遍"判定这条,审计确认 `requirements_master §6:102`、`prd §5.10.6:1052-1053` **文档原本就写对**(2遍/count-based),这次改动修的是代码(`isLessonComplete()`)跟文档之间的漂移,不是文档本身有误。
- 出处:本次对话(App Claude 会话,2026-07-12)+ `docs/juexue_v2_design/reference/预科19届学修大纲-原文提取.txt`(93次/开卷72/闭卷60/30分等绝对数出处)+ `docs/design_v2_decisions.md`(决策012/017/091/120/125/157/160、延后-4)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D4. 语义搜索(决策187)v1.0 明确不做,App 端定位为"纯消费方"(2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:2026-07-12 一致性审计(见上 D3 附带发现)挖出决策187(2026-06-26·"记录搜索暂缓,上线前做")从未闭环——sss-dev 仍缺 4 个语义搜索 DB 对象(`search_chunks`/`search_semantic`/`search_log`/其 `top_distance` 列),App 端(`lib/`+`app/`全仓搜索)也完全没接这个 rpc。决策187 原文本身留了"或明确接受降级"的出口(现有 catalog 页课名客户端过滤可顶着上线),但 2026-07-10 的 v1.0 首发范围切分(D1)完全没提这条,不清楚当时是否已默认接受降级。已向 PM 核实原委(本次对话逐条讨论"剩余项+延后项"),PM 拍板。
- **决策**(PM 2026-07-12 对话中拍板):语义搜索的搜索逻辑/embedding 数据由**官网/ETL 线提供**,App 侧只做"消费方"(调 `rpc('search_semantic', …)` + 结果页),不写搜索算法、不管 embedding 灌数据;**当前(v1.0)阶段明确暂不做**——不等 ETL、不主动排期,catalog 页维持现有"课名客户端过滤"方案上线。
- **应改为**:决策187 的"⚠️上线前必做"标签改为"v1.0 明确不做,待官网/ETL 线接口就绪后再排期对接(App 侧只是消费方)";D1(发布范围切分)记录里补一条,说明这条已核实处理过,避免以后又被当漏项重新翻出。
- 出处:本次对话(App Claude 会话,2026-07-12)+ `docs/design_v2_decisions.md:1164-1189`(决策187)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D5. 考试豁免(60岁免考等)已提前到 v1.0 完成,延后-7 拆细(2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:PM 逐条过"剩余项+延后项"清单时追问延后-7("共修93次自动算 / 60岁豁免 / 法王祈祷文独立核对",`design_v2_decisions.md:57`),核实发现代行记录表 `proxy_action_records`(决策121)本就支持 `action_type=exempt`+`target_kind=exam`(种子数据甚至已有"年龄豁免"示例),只是零 UI 消费方、考试合格判断也从不查这张表。
- **决策**(PM 2026-07-12 对话中拍板,分两步):① 共修93次自动算(供考试合格线分档用)保留系统自动数,不改回 admin 人工填(理由:数据源与全站其它出勤统计口径一致,人工数有漏排除临时场次的出错风险——本轮就出过一次这类失误)。② 60岁免考:**豁免视为通过,但须标注具体豁免的是哪一项**(reason 必填留痕)。
- **已落地**(工程侧):管理端升学详情页新增"标记豁免"表单(复用既有 `useRecordProxyAction`,零迁移);`useStudentAdvancement`/`useAdvancementRoster` 查到 exempt+exam 记录时 `examIsPass` 恒 true 并显示豁免原因。已用 sss-dev 真实数据验证写入/读取路径,测试数据已清理。commit `a20ae47`。
- **应改为**:中枢延后清单需把延后-7 拆细为三项:"共修93次自动算"标**已提前到 v1.0 完成**(与延后-4 拉齐,同款"规则已定+这轮把工程做了"处理);"60岁免考"也标**已提前到 v1.0 完成**;只剩"法王祈祷文独立核对"一项仍延后 v1.5+。`requirements_master §5.10.8` 对应措辞一并订正。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项+延后项"+独立 agent 审计)+ `docs/design_v2_decisions.md:57`(延后-7)+ `docs/requirements_master_2026-05-31.md:1083-1085`(§5.10.8)+ `docs/design_v2_decisions.md:2907-2932`(决策121)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D6. 学期末一站式工作流(延后-8)已全套完成(2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:PM 逐条过"剩余项+延后项"清单时追问延后-8("学期末一站式工作流",`design_v2_decisions.md:58`),查明底层能力有真缺口(不只是"包一层UI"):留级"两种形态"(决策018:留原班重修/转下一届)此前只有前者;旁听"继续旁听/转正式/离班"三选一此前完全没有。⚠️ **引用订正(2026-07-12 独立 agent 审计发现)**:这条旁听三选一逻辑最初误引"决策017"——核对 `design_v2_decisions.md` 发现决策017 全部约25处出现均是"升学/留级/毕业人工判定"这条,与旁听无关;旁听三选一实际是**决策018**表格里"旁听者跨学期"这一行(该行末尾"(017)"是决策018原文自己列的**问题017**编号,不是决策017本体),已同步订正代码注释(`lib/mutations/advancement.ts`/`app/(admin)/advancement/semester-end/[cohortId].tsx`)。
- **决策**(PM 2026-07-12 对话中拍板):**全套做**(不做"精简版"——精简版原计划只做旁听三选一+引导壳,留级维持只有"留原班重修"一种,"转下一届"单独排期)。
- **已落地**(工程侧,零迁移,全复用既有RLS/RPC):新增管理端「学期末处理」页(`app/(admin)/advancement/semester-end/[cohortId].tsx`,从班级详情页"管理操作"进入)。"转下一届"=新写 `useRecordHeldBackTransfer`(旧班标held_back+撤主班→新班建成员行+切主班+留痕to_cohort_id→调用既有 `provision_cohort_vows` RPC 重建功课,内加行等限时愿从新班起算,决策018补充);"留原班重修"=既有 `useRecordAdvancement` held_back 分支不变。"继续旁听"=新写 `useRecordAuditorContinue`(纯留痕,decision='other');"转正式"复用既有 `useUpdateMemberRole`;"离班"复用既有 `useRecordAdvancement` 的 left。已用 sss-dev 真实数据(hf 自己的班级归属+一个临时测试班)完整验证转班序列(含"先撤旧主班再插新主班"避开 `uniq_class_members_primary` 唯一索引的顺序)+功课重建+继续旁听留痕,全部清理、hf 原始状态已核对完全还原。commit `2d7f53c`。
- ⚠️ **独立审计顺带确认(非本轮新问题,已有既定裁决)**:留级次数上限——`useRecordHeldBackTransfer`(和既有 `useRecordAdvancement`/`classes.ts` 一样)不做系统硬性次数校验,只累计 `held_back_count`。核对发现决策090原文写的是"v1.0做系统校验",但本清单 **B6b(PD-18,2026-07-08已裁决)** 已改判"以 requirements_master/schema 为准 = admin 人工掌握、v1.0 不系统硬卡"——本轮代码不做校验是**遵循 PD-18 这条既定裁决**,不是新缺口;但 `feature_inventory_v2.md §2.10` 仍写着决策090原文"v1.0系统校验"未订正,回中枢时可一并处理。
- **应改为**:中枢延后清单需把延后-8 标为**已提前到 v1.0 完成**。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项+延后项"+独立 agent 审计)+ `docs/design_v2_decisions.md:58`(延后-8)、`:784-822`(决策018/检查018)、`:3760`(决策099)、`:4104`(决策090)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D7. 出勤85%/70%健康色阈值(选项B·去色,2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:D3(2026-07-12)遗留"仍待"项之一。PM 逐条讨论"剩余项"时追问,核实教学大纲原文(`docs/juexue_v2_design/reference/预科19届学修大纲-原文提取.txt`)全文无出勤率百分比口径,唯一出勤规定是"共修出勤总次数≥93次"这个绝对数(用于考试分档,D3 已实现),85/70 两个百分比数字对不出任何业务依据,是团队为升学页"要有个颜色"自造的启发式。
- **决策**(PM 2026-07-12 对话中拍板,三选项 A维持/B去色/C改用93次绝对数分档 中选 **B**):升学评定页(`app/(admin)/advancement/index.tsx` + `[studentId].tsx`)不再按 85/70 着色,改中性色纯数字展示。
- **已落地**(工程侧):`lib/admin-thresholds.ts` 删除 `ATTENDANCE` 常量(DB 侧关怀名单 `attendance_dim_lag` 同口径的独立硬编码 85/70 是不同消费方,本次不动,教务定稿延后-5 时那边仍需单独处理);两消费文件出勤率数字/进度条改中性色(`INK`/`INK3`);一并去掉详情页②出勤卡片单独硬编码的 `rate<70` 警示边框触发、确认弹窗五维摘要②出勤行的 ok/✗ 判断(同一决策逻辑的一致延伸)。commit `96401dc`。
- **应改为**:D3"仍待"项已划除此条(见上 D3 订正行);中枢若另有"占位阈值待核"清单登记出勤85/70,一并标记为"已去除,不再是待定阈值"。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项")+ `lib/admin-thresholds.ts` + `docs/juexue_v2_design/reference/预科19届学修大纲-原文提取.txt`(93次绝对数出处,行622-626)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D8.「圆满课次」扩展(选项C·不新定本周口径,2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:D3(2026-07-12)遗留"仍待"项之一。技术调研发现这不是"加一列"级别的小改动:现有 `get_cohort_week_totals` 的"本周"= 日历周(周一对齐,`date_trunc('week',...)`);课程进度算法 `get_current_week_number`(决策157/§12.6.5)的"本周"= 滚动周(跟着 `cohort.start_date` 滚动7天一轮),两套定义在非周一开班的班级会不重合;此外"一周若排多节课,圆满课次算ALL(全完成)还是ANY(完成一节即算)"这个产品口径此前没有任何文档明确过(`lib/queries/classes.ts:507` 注释只泛泛写"待教务口径定"、`tests/casebook/report-aggregation.md` RA-2 讨论的是单课时圆满规则本身已定、只差自动判定工程量——两处均未具体谈及"一周多节课"这个维度;ALL/ANY 这个问题是本轮技术调研中新发现、需要追加拍板的产品口径缺口,不是这两处引用原本就写明的待定项)。
- **决策过程**(PM 2026-07-12 对话中拍板,分两轮):①先选"日历周"(vs滚动周);②但深入代码后发现同一个师兄端班级页(`app/(student)/class.tsx`)"本周课程"板块本身就是用滚动周算的(`get_current_week_lessons`)——若"圆满课次"另走日历周,**同一屏幕**会出现两个"本周"对不上号的课次(不是抽象风险,是可视冲突),遂改按**选项C**:不新定义"本周"、不做ALL/ANY合并判断,直接给 class.tsx 已展示的"本周课程"清单**逐节**加"全班完成人数"——天然跟同页面口径一致。
- **已落地**(工程侧):新增 `get_cohort_lesson_completion` RPC(security definer 越RLS读全班,只出聚合计数不出个人·#193,模式同既有 `get_cohort_week_totals`);`lib/queries/classes.ts` 新增 `useCohortLessonCompletion`;`app/(student)/class.tsx`"本周课程"每行接入"全班N/M人已圆满"。本地SQL harness新增LCC-1/2/3(复用14_completion_views.sql既有stu_b/L4夹具,18/18通过)。**顺手修了一个既有生成器bug**:`scripts/gen-types-from-migrations.cjs` 的数组类型函数参数(如 `uuid[]`)此前被误生成为 `string` 而非 `string[]`(`tsScalar` 的数组后缀检查排在标量前缀检查之后,逻辑上走不到)——`has_class_role`/`my_admin_cohorts` 两个既有函数一直中招,只因从未被客户端 `rpc()` 调用而未曾报错,这次新函数会被调用故顺手在生成器根子上修了(非本函数专属workaround)。commit `84d7485`。**此 migration(`20260712000500_cohort_lesson_completion.sql`)尚待应用到 sss-dev,应用后需 REST 实测确认。**
- **应改为**:D3"仍待"项已划除此条(见上 D3 订正行);`lib/queries/classes.ts:11` 附近旧注释"待 PM 定口径后单独接"已同步更新。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项")+ `supabase/migrations/20260712000500_cohort_lesson_completion.sql` + `app/(student)/class.tsx` + `lib/queries/classes.ts` + `supabase/migrations/20260618000030_scheduling_and_progress.sql`(滚动周算法出处)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D9. 找回密码(选项B·完整方案,2026-07-12 新记——真决策,需 PM 在中枢正式登记编号)
- **背景**:PM 逐条讨论"剩余项+延后项"清单时追问,核实发现 App 全仓(`app/`+`lib/`,以及本 App 自身的规划文档——`design_v2_decisions.md`/`requirements_master_2026-05-31.md`/`prd_2026-05-31.md`/`schema_phase1_2026-05-31.md`/`rls_policies_2026-05-31.md`)对 `resetPasswordForEmail`/"忘记密码"/"找回密码"零命中,确认**本 App 此前没有任何找回密码功能或规划**(仓库里另有 `docs/juexue_v2_design/` 提到"找回密码"能力,但那是另一个系统"觉学"的只读参考文档——迁入时的提交就自陈"只读参考",描述的是本仓不存在的 `backend/src/modules/*/routes.ts` 结构,与本 App 无关,已排除)。但实际不算彻底无路可走:`app/(auth)/verify.tsx` 登录第2步的密码/验证码二选一里,验证码模式走 `signInWithOtp`+`verifyOtp`,技术上已能让忘记密码的人凭邮箱验证码重新拿到 session——只是(1)该切换链接原文案"用验证码登录"框定为"换种登录方式",不是"找回密码"的信号,真正卡住的用户未必会点;(2)进去之后没有任何地方能设新密码,只能永远靠验证码登录,登录页密码框还继续默认摆在最前面。
- **决策**(PM 2026-07-12 对话中拍板,三选项 A纯文案/B完整方案/C不做 中选 **B**):文案上明确该链接是找回密码路径 + 验证码登录成功后给一次性机会设新密码。
- **已落地**(工程侧,零迁移、零schema改动,纯前端):`app/(auth)/verify.tsx` 密码模式下切换链接改名"忘记密码?用验证码登录";新增 `app/(auth)/set-password.tsx`——验证码登录成功后先转到这一页(可跳过、不强制),调用 `supabase.auth.updateUser({password})` 设新密码;密码模式登录成功不受影响,仍直接进入口闸门。已用 Playwright 在 web 预览实测:两屏渲染正确、密码长度/两次一致性校验生效、无 session 时提交优雅报错不崩溃、跳过按钮正确路由到入口闸门。commit `6d1f70b`。
- **未能验证**:真实验证码收发的完整端到端流程(需要能收邮件的真实邮箱,沙盒环境收不到邮件)+ 真机上的实际体验,这两块需 PM/QA 在真机或可收信环境走一遍确认。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项")+ `app/(auth)/verify.tsx` + `app/(auth)/set-password.tsx`。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

### D10. 设置页推送开关——决策188投递方式定案为方案A(2026-07-12 新记——真决策,尚未开工,需 PM 在中枢正式登记编号)
- **背景**:决策188(`design_v2_decisions.md:1196`)提出"每班一条周提醒"(辅导员在管理端配置,非用户个人 user_settings 开关),原文标题即带【方案待 PM 选投递方式】,列了两个未决方案:A(服务端推送:pg_cron+Edge Function 定时扫描+调 Expo Push API,复用已建的 `user_push_tokens` 表)/B(客户端本地通知,需装 `expo-notifications` 且要处理设备时区≠班级时区换算)。`app/settings.tsx` 的推送开关此前已被"审计 P0 清空承诺"(2026-07-02,`settings.tsx:34`)主动撤——v1.0 无推送系统,开关是纯本地假承诺,整区改静态"即将推出"提示,`Toggle`/`IosSwitch` 组件代码仍留着(未接入,为 v1.5 恢复留的死代码)。DB 侧 `user_push_tokens` 表(`20260618000090_aux.sql`)虽已建好(含 RLS),但是孤儿基础设施——零代码写入、未装任何推送相关依赖、无发送端 Edge Function/cron。
- **决策**(PM 2026-07-12 对话中拍板,分三点):
  1. **投递方式选方案A**——App Claude 建议理由:B 方案"设备时区≠班级时区换算"的麻烦在 A 方案下不存在(服务端直接按 `cohort.timezone` 判断,跟本系统其它计算方式一致);B 依赖用户已打开过 App 才能排上本地通知,恰好覆盖不到真正容易忘记、需要被提醒的那批人,削弱提醒功能本身的意义;`user_push_tokens` 表已经是为 A 方案建的,选 B 会让这份基础设施继续闲置。
  2. **覆盖面要求**(PM 原话"每个人都可以接收到消息才对"):推送权限注册不能藏在设置页深处等用户自己找,登录后要主动弹系统权限请求,让每个人都有机会同意;维持"一个班一条"班级级别配置,不做个人 user_settings 开关这层(即决策188原范围,不扩大)。
  3. **默认提醒文案由 App Claude 拟定**(PM 原话"你来写文案,如果不合适再改"):"本周共修时间快到了,愿大家精进闻思,同沾法喜。"——辅导员在管理端配置时可覆盖为班级自定文案。
- **尚未开工**(PM 2026-07-12"记录这个方案,记录为待办,先进行下一项"):完整方案盘点共5块——①`cohorts` 表加4字段(`reminder_enabled`/`reminder_weekday`/`reminder_time`/`reminder_message`,决策188原方案,含上述默认文案)②客户端装 `expo-notifications`,登录后主动请求推送权限、写入 `user_push_tokens`③新 Edge Function + pg_cron(建议每15分钟扫描一次,判断是否有班级"到点了",到点则群发该班活跃成员的 Expo Push,同班同天防重发)④管理端班级设置页新增"学习提醒"配置区(开关+星期+时间+文案)⑤`app/settings.tsx` 的静态"即将推出"换成真实状态(已开启/未开启+手动开启按钮,给拒绝过系统权限弹窗的人一个后路)。工作量评估 **Large**(本轮"剩余项"逐条讨论里最大的一项)。App Claude 已明确提示:真机推送到达情况(是否真收到通知)本沙盒环境无法验证(无真机/无法收推送的模拟器,同音频下载真机验证缺口同类限制),且 `expo-notifications` 生效可能需要改 `app.json` 原生权限声明,最终生效需重新构建开发版才能测。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项")+ `docs/design_v2_decisions.md:1196`(决策188)+ `app/settings.tsx:34`(审计P0清空承诺注释)+ `supabase/migrations/20260618000090_aux.sql`(`user_push_tokens` 表)。**这条决策编号需 PM 回中枢按序分配**,App 侧不自行编号。

---

## E. 待办跟进(非新决策,不需编号——只是不能让它悄悄丢的行动项)

### E1. 老学员内加行迁移处理(延后-24)——等 PM 提供老库数据后比对
- **现状**:`design_v2_decisions.md:69`(延后-24)+ `:4569-4602`(决策076/检查076)已把这条明确挂为"随老数据迁移(独立 DBA 项目)方案一并定"——老 Supabase 历史数据迁移本就"明确不在本 App 范围"(决策076 原文),App 侧目前没有任何真实老学员起修日/历史修量数据可供判断"谁的内加行按真实 start_date 已过 4 年"。本轮"逐条讨论剩余项"过一遍,确认这条现在**卡在外部数据,不是技术选型/工程量的问题**,讨论"过期了怎么处理"在拿到真实数据前都是纸上谈兵。
- **PM 表态(2026-07-12)**:"这个真正需要标记待办。我会提供目前的db然后进行比对。"——即 PM 后续会提供老系统数据库(或导出),届时需要:①拿老库真实数据核对每位老学员的内加行真实起修日/已修量 ②据此判断谁已过4年限时锁定、谁尚在窗口内 ③再决定过期学员的处理政策(是否网开一面/重新起算/其它)——这三步都要等 PM 给数据才能推进,现在没有可动工的东西。
- **待办**:PM 提供老库数据后,回来找这条继续处理(核对+定政策+按结果决定是否要工程改动)。App 侧本轮无代码改动。
- 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项+延后项")+ `docs/design_v2_decisions.md:69`(延后-24)、`:4569-4602`(决策076/检查076)。

### E2. 账号注销定时清理——Edge Function 未部署 + cron 定时任务当前处于未注册状态
- **现状**:D1 提到的"账号注销真实接线"里,DB 层(标记/撤销/RLS)+ Edge Function 源码(`supabase/functions/purge-deleted-accounts`)均已完整写好,但 Edge Function **尚未部署**到 sss-dev(需 Supabase CLI + service_role key,App 侧无此权限,需 Eric 或 PM 自己有 CLI 授权的环境来跑)。
- **2026-07-12 发生的插曲**:PM 尝试在 sss-dev SQL editor 手动配置 `cron.schedule(...)`(定时敲 Edge Function 端点)时,两次都把迁移注释里给的占位符示例(`<PROJECT_REF>`/`<ANON_OR_SERVICE_KEY>`)当字面值直接执行,第二次虽换上了真实 key 但用的是 **anon key 而非 service_role key**(Edge Function 鉴权比对的是 `SUPABASE_SERVICE_ROLE_KEY`,anon key 会被 401 拒绝),且两次 URL 都保留了尖括号或占位文字未真正替换。发现问题后已引导 PM 执行 `cron.unschedule('purge-deleted-accounts-daily')` 撤销第一条无效任务;**第二条(job id=2,配置同样无效)当时约定"跳过、待 Edge Function 部署时一并重配",尚未撤销/修正**。
- **待办**(顺序不可颠倒):①Edge Function 部署(`supabase functions deploy purge-deleted-accounts` + `supabase secrets set SUPABASE_SERVICE_ROLE_KEY=<sss-dev 真实 service_role key>`,需 Eric 或有 CLI 权限者执行)②部署确认成功后,先 `cron.unschedule('purge-deleted-accounts-daily')` 清掉当前失效的 job id=2,再用真实 project ref(`ubyzyadlzmtgxvbxanbr`)+ 真实 service_role key(不带尖括号)重新跑一次 `cron.schedule(...)`。
- 出处:本次对话(App Claude 会话,2026-07-12)+ `supabase/functions/purge-deleted-accounts/index.ts` + `supabase/migrations/20260710000000_account_deletion.sql`。

### F1. B线19条业务语义审计(2026-07-14 完成)新发现3处引用/口径缺口——详见App repo `docs/测试计划_技术功能与业务语义两线_2026-07-13.md`
- **①「整本课圆满」聚合规则从未独立拍板**:`v_course_completion`(全部课时AND=整本课圆满)代码行为正确,但这条聚合规则本身只是实现者从决策091(逐课时规则)自然外推,从未被单独决策过。建议:若将来出现"某课时可选/不计入圆满"这类产品需求,先补一条正式决策,不要让实现者继续沿用推断。
- **②「功课圆满」引用的"决策091.2"实际不存在**:`v_program_practice_completion`/`v_program_practice_summary` 代码行为正确("缺失愿≠已达标"陷阱已避开),但此前文档/注释里引用的"决策091.2"经全文检索只对应闻思逐课时规则,跟功课/愿完全无关,是误引。真正依据只是视图自己的头注。建议:补一条可追溯的正式决策,或订正引用。
- **③关怀5维「出勤维」(SM-7)只实现了决策181的一半**:决策181要求"只算正式期(formal_since起)+只算记考勤场次(tracks_attendance)",`attendance_dim_lag` 只做了后半句,没排除旁听期出勤——代码自己的头注承认("SD-6待细化"),原因是缺一个干净的"转正时间"时间戳列,不是遗漏。需要 PM 决定:补列并接上,还是先接受现状(现状不算错,只是不完整)。
- 出处:2026-07-14 App Claude 会话系统性核查B线19条业务规则(4个并行子任务分别核对圆满判定家族/学籍资格类/考核关怀座次类/遗留占位幂等类),另发现并已直接修复2处真代码bug(不需回中枢,已在App repo处理):`lib/queries/daily.ts` 班级模式算"本周"误用设备本地时间(commit `1a412b4`)、`record_study`幂等查重并发原子性(commit `3987203`)。

---

## 附:本轮已在 App repo 就地处理、无需回中枢的收尾(仅备查)
- 陈旧断言数字(counting.md 41/173、state-machine.md 182、TESTING.md 129)先前已更新;**2026-07-10 再次核对发现 TESTING.md 两处仍写着已过期的"287"**(287 是 07-08 当天的值,07-04~07-09 那批迁移应用+当晚 cohorts_select 修复后已涨到 300),已顺手更正为 300,和 TESTING.md 正文改动一并处理,不单开 commit。
- 7 处过时注释(run.mjs/run.sh/supabase-local.sh/db-rls-tests.yml/08:152/weekly_session_count/quick-count)已清理。
- 各测试文件已加「期望断言数」哨兵防静默漏跑;`get_cohort_care_dims` 已补测试。
- care.ts 时区已改为按 cohort.timezone(§1 合规)、practice_dim_lag 已排除自选 custom 愿(XJ-10)。
- **2026-07-12 弱网/离线6阶段(纯工程部分,不涉及产品口径,不需回中枢)**:①`practice_logs` 弱网写入幂等(client_token,见上 C3)②NetInfo 接入 TanStack Query onlineManager + 全局离线提示条 + 按幂等性分级的 mutation 重试策略③后台"撤销自学资格"补二次确认 + 5 处列表分页④36 屏 loading/error/empty 三态排查中发现 12 个"按id查详情"hook 把查询失败和"没这条记录"混为一谈(`if(error||!row) return null`),已改为 error 先 throw——纯代码健壮性修复,不涉及产品口径⑤CI 排查一次 e2e 偶发失败(④班级本周课→⑤课时学习),复跑同 commit 即过(非本轮代码回归),顺手给 `e2e/smoke.spec.ts` 补上 `e2e/boundary.spec.ts` 早有的 `waitForTimeout(500)`(乐观写后查库的既有先例,此前唯独这条漏加)。
- **⚠️ 2026-07-12 追加:上一条⑤当时"复跑即过"的诊断不完整——真正根因今天(同日晚些时候)才查清**:同一条"④班级本周课→⑤课时学习"e2e 断言又超时,这次两次复跑仍稳定复现(排除偶发),深挖发现 `e2e/global-setup.ts` 的 `mondayOf()` 用 CI runner 本地(UTC)时钟算"周一",与 cohort.timezone 硬编码的 `Asia/Shanghai` 不一致——UTC 16:00-24:00(上海已跨入下一天)这段算出的"周一"晚了一整周,导致排的第1周课时被判成"不是本周"。已改成先按 Asia/Shanghai 解出"今天"、再回溯周一(同 `lib/date-tz.ts::todayInTz` + `report-week-calc.ts::mondayOnOrBefore` 的两段式做法),独立脚本核对同一时刻新旧算法结果确实差一周。**纯 e2e 测试基础设施 bug,不涉及 app 代码/产品口径**;顺带确认了另一次真正的 CI 基础设施不稳定(重跑一个本已通过的更早 commit 复现出完全无关的 Supabase edge-runtime 容器 Bus error 崩溃),证实当时 CI 环境本身有状况,非代码回归。commit `246d068`。
- **2026-07-12 "剩余项+延后项"逐条讨论中,另两条查完确认无需动作(非待办,备查)**:
  - **延后-15(多班并行师兄的一天)**:查证底层功能其实已经有——`class.tsx` 顶部"切换主班"入口(`myCohorts.length>1` 才现)+ 弹层选班(2026-06-30/07-01 PM 已定案迭代);打卡按愿归属、不按班选(消除多班混选)。延后-15 剩下的只是"这个切换式体验是不是最优呈现"这种纯视觉打磨,不是功能缺口。**PM 拍板:维持现状**,不投入打磨。
  - **延后-19(「末学」tab 导航结构+愿分组)**:查证这条已被**决策158**(2026-06-19,晚于延后-19 原提出时间)绕过——「我的」(=末学)已从 Tab 移到首页左上头像点入的 `/me` drill-in 页(决策158"底部tab减为4:首页/闻思/修持/班级"),"愿列表分组方式"也已经在"修持"tab 实际做出来了。延后-19 提到的"Claude建议方案(导航五项)"当时只存在于聊天记录、未存进repo,现已不可考,但已不需要——问题本身已被后续决策+实现绕过。**PM 确认**("我的页面入口在首页"),视为已解决,无需再处理。
  - 出处:本次对话(App Claude 会话,2026-07-12,PM 逐条讨论"剩余项+延后项")+ `docs/design_v2_decisions.md:60`(延后-15)、`:4326-4344`(排版细节延后-15)、`:64`(延后-19)、`:1651-1667`(决策158)。
