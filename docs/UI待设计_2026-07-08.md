# UI 待设计 / 前端待施工清单（2026-07-08）

> 性质:App 前端【需 UI 设计 / 前端施工】的集中台账。来源=2026-07-08 全景审计 + 一致性核查(工作流 wxe8595xm)逐条回代码核实。
> 用途:判例先行的「计算层」已全部闭环(加行/净土/入行/学经/横切/状态机/关怀/报数 + 30 条 PD 全裁决),剩下的主要是**前端接线**——本清单把散落在判例/审计里的 UI 项集中,供前端排期。
> 状态图例:🔴 本轮决策已定待落地 / 🟡 数据层已就绪待接线 / 🟢 v1 占位待接真 / ⏳ 待教务外部。

---

## A. 本轮 PD 裁决产生的 UI（决策已定·待前端落地）

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| A1 ✅ **PD-20 自选经/抄经/拜经选择器** | 加功课识别「自选经/抄经/拜经」类时,从 `program_optional_practices` 清单按专业选具体经,**替换现自由文本输入**(违决策052) | 已完成(波C·Part3):AddVowModal 加来源切换,自选经模式锁清单+锁经名,不可自由输入;一般自定愿(其它自定愿)维持完全自定义原样 | app/(student)/practice.tsx |
| A2 ✅ **PD-19 心经 1-9 遍硬校验**(2026-07-10 完成) | 配置端 + 师兄自调端都硬校验每日目标限 1-9,越界(如12)拒绝 | 数据驱动实现(不写死"心经"专属):师兄自调 PaceSheet 读 `practices.allowed_daily_targets`,有白名单则改选择器(不给自由输入),无白名单维持原 >0 校验;若 `daily_target_locked`(如净土)则整块隐藏输入、显示"已锁定"说明。配置端(管理端纠错入口)已在 R1 一并完成同款白名单选择器。真机核对时发现并修了一个边界:老数据里存在锁定前遗留的非白名单值(如净土某些愿 daily_target=1000),预选逻辑已加保护,不会让"什么都没碰就保存"把非法旧值悄悄传回去 | app/vow/[id].tsx; lib/queries/practice.ts |
| A3 ✅ **SD-5 师兄端状态色(含掉队)展示**(2026-07-10 完成) | 撤173 后 PM 定师兄可见自己状态色含 falling_behind 掉队提示,做师兄端状态展示 | 已接线:`get_vow_status()` 读时算(非存储列,躲开"Edge cron从未真正跑过"的坑),修持页(practice.tsx)功课卡片/座次行用色点提示掉队(琥珀)/告急(红)、克制(on_track不额外提示);功课详情页(vow/[id].tsx)显示完整文案。已用 sss-dev 真实愿数据核对 RPC 返回值符合预期 | tests/casebook/state-machine.md:103; app/(student)/practice.tsx; app/vow/[id].tsx; lib/queries/practice.ts |

## B. 管理端接线（数据层已就绪，未连线）

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| B1 ✅ **后台学员详情/列表 接 5维+关怀日志**(2026-07-10 完成) | students/[id].tsx 与 index.tsx 接 `useStudentCare`/`useCareRoster`(已实现·care 专页在用) | 抽共享组件 `StudentCareDims`/`StudentCareFollowups`(同 StudentTransmissions 先例,自带数据不吃 props),三处消费:care/[studentId].tsx(原样式,顺手去重)、students/[id].tsx(窄屏详情页)、students/index.tsx(宽屏 Master-Detail 面板)——三处此前各占位或各写一份,现单一真源。跟进记录写权按 RLS 算 zhumai/aixin(非 isAdmin) | components/admin/student-care-panel.tsx; app/(admin)/students/[id].tsx; app/(admin)/students/index.tsx; app/(admin)/care/[studentId].tsx |
| B2 ✅ **RA-1「当日在修人数」卡片**(2026-07-11 完成) | 首页/班级卡接 `get_cohort_today_active`(已建·授权·测过) | 新 hook `useCohortTodayActive`(同 `useCohortWeekTotals` 降级口径:未部署/出错→null,不挂占位);接入班级页(class.tsx)顶部"共修成就"区,与既有 2 个本周数字并排为第 3 个"今日在修"数字,当日 0 人不显示(同本周口径,不挂空「0」)。已用 hf 真实登录 REST 核对 RPC 返回形状 `{active_count, today}` 与字段解析一致;`check-orphan-rpcs.mjs` 白名单已移除该函数(不再是孤儿) | lib/queries/classes.ts; app/(student)/class.tsx; scripts/check-orphan-rpcs.mjs |
| B3 ✅ 后台管理2处修复(2026-07-11 一致性调研发现·完成) | ①"撤销自学资格"缺二次确认 ②5个列表无分页,数据涨过 PostgREST 单次封顶会静默截断 | ①`students/index.tsx`+`students/[id].tsx`两处撤销按钮补 `confirmAsync`(此前是本仓库"撤销/删除"类操作里唯一没有二次确认的)。②抽共享分页函数 `lib/queries/paginate.ts`(`fetchAllPages`,单一真源,`useAdminQuestions` 原有的内联分页循环也改用这份),应用到 `useAdminStudents`/`useAdvancementRoster`/`useAdminFeedback`(原 limit 500 硬顶)/`useAdminSelfStudyStudents` 四处;审计日志(`useAuditLogs`)改 `useInfiniteQuery` 真分页+「加载更多」按钮+搜索框(此前硬顶200条、查不到更早记录也搜不了,是最明显的一处) | lib/queries/paginate.ts; lib/queries/audit.ts; app/(admin)/audit/index.tsx; lib/queries/admin/students.ts; lib/queries/advancement.ts; lib/queries/admin/feedback.ts; lib/queries/admin/selfstudy.ts; lib/queries/admin/quiz.ts; app/(admin)/students/index.tsx; app/(admin)/students/[id].tsx |

## C. 学员端功能（v1 占位/未接真）

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| C1 ✅ **学修档案 dossier 六维**(2026-07-11 完成) | 闻思圆满%/咒数/座次/答题/讲考/出勤 接真 | 新文件 `lib/queries/dossier.ts` 五维数据层(第六维"听读足迹"已在 footprint.ts)。3 条口径经 PM 拍板:①咒数/座次终身累计(含已换/已完成的愿,非只算 active);②出勤历史所有班级累计(查设计文档核实——决策123/4821:主班仅默认视图偏好、无统计含义,与 footprint/useMyVows 既有口径一致);③闻思圆满做百分比,但分母改用真实 `course_lessons` 行数(实时查询),不用 `courses.total_lessons` 那个运营手填可能失修的静态列。⛔ 讲考维度只显参与次数,绝不查 `speaking_evaluations`(决策067"克制"·该表 RLS 本就没有 user_id=auth.uid() 分支,师兄本人也读不到评价)。已用 hf 真实登录核对各条查询真实可跑(200,含验证一个此前全仓库未用过的 PostgREST 语法:对内嵌资源做 `.in()` 而非 `.eq()`);hf 账号本身无学员活动数据,故未做完整多课程端到端断言,但业务逻辑逐行照抄已在 course/[id].tsx 生产验证过的 `useMyCourseLessonStatus` 判定规则,只是从单课扩到批量多课 | lib/queries/dossier.ts; app/dossier.tsx |
| C2 ✅ 通知系统(站内收件箱)+ 首页未读红点(2026-07-11 完成) | notifications 表接真 + 未读计数 + 标已读 + 跳转;真实未读驱动铃铛红点 | 调研确认:决策062/068/170 定义四类(学修/班级/法会/系统)+ UI 壳早就建好(筛选/分组/已读态/空态),只差真数据。范围按 PM 拍板拆两块——**本轮只做站内收件箱**(不卡人):新建 `notifications` 表(RLS:读=本人+admin;改=本人;建=admin 或任意 class_admins 身份,复用同一张表的 EXISTS 判定、纯学员不可插防滥发)+ 查询(列表/未读数)+ mutation(标已读/全部已读)+ 接入 `app/notifications.tsx` 真数据 + `home.tsx` 真实红点。写入端本轮只接 3 个"动作触发即写"的事件源(入学审批通过/拒绝、转正、班级公告发布,均在各自已有 mutation 里顺手加一次 `createNotification` 调用,失败不阻断主动作);"新一周课程/待复习/断签/每周回向"这类时间型事件需要 cron 定时派发基建(同 SD-4 未建),明确不在本轮。**真正的 push 推送(决策188 方案A/B 待PM选)+ C3 设置页开关未动**,等投递方式选型定了再排。本地 SQL harness 新增 7 条断言(class_admins 身份可插/纯学员拒绝/本人可读写自己的/看不到他人的/admin 兜底),顺手订正一个历史哨兵漂移。`notifications` 表已由 PM 手动过 sss-dev dashboard 应用(本 session Supabase 连接器不可用);`check_schema_parity.py` 确认线上表已在场、无漂移;已用 hf 真实登录 REST 核对列表/未读计数/标已读/清理全链路,字段形状与前端代码完全对上 | supabase/migrations/20260711000000_notifications.sql; lib/queries/notifications.ts; lib/mutations/notifications.ts; lib/queries/admin/students.ts; lib/mutations/classes.ts; lib/mutations/events.ts; app/notifications.tsx; app/(student)/home.tsx |
| C3 🟢 设置页推送开关 / user_settings | 恢复 Toggle 接 user_settings(推送开关/偏好) | 通知区改「即将推出」死标签,Toggle 组件已写未接 | app/settings.tsx:14,32-39 |
| C4 ✅ **账号注销(Apple 合规·上架硬要求)**(2026-07-10 完成) | 注销按钮接 Edge Function + 合规删除 | 已接:自助申请→30天保留期软删(占位·待核)→期满 Edge Function `purge-deleted-accounts` 真删;找回仅限后台管理操作(无自助撤回),纠正了 decision078 原「Edge Function 删 auth.users」(纯硬删)与本页文案「档案永久保留」的矛盾。**Eric 待做**:部署该 Edge Function + 设 service_role 密钥 + 手工执行 pg_cron/pg_net 排程 SQL(迁移文件尾部已注释好) | app/delete-account.tsx; supabase/migrations/20260710000000_account_deletion.sql; supabase/functions/purge-deleted-accounts/ |
| C5 ✅ 首页主修上下文切换(2026-07-11 补完) | 按主修上下文在班级/自学模式切卡片文案 | ⚠️ 2026-07-10 核实纠正:此前登记「get_primary_context RPC 前后端都不存在」**已过时**——`usePrimaryContext()`(`lib/queries/self-study-progress.ts`)其实早已建成并在 daily.tsx 真实使用(client 端聚合 class_members/cohorts,非 DB RPC,一样能用)。「每日功课」卡已接真实本周课数;**2026-07-11 补完最后一块**:「共修法会」卡文案按 `ctx.mode` 分流(班级="共修法会·发愿回向"/自学="平台法会·发愿回向",自学不提"共修"二字,决策144 口径),纯文案分流复用既有 hook,不新增查询 | app/(student)/home.tsx; lib/queries/self-study-progress.ts |
| C6 ✅ 「继续学习」续学定位(2026-07-10 核实,同 C5/C7 一并查出) | 首页续学卡按进度定位本课 | 此前登记"注释与代码不符"其实是注释过期——href=/daily 不是权宜之计,/daily 页面内部本就按 `usePrimaryContext()` 真实定位(班级扇出取本周课/自学按节奏取本周计划),点进去就是当下该学那节,不需要在首页卡片这层再重复定位一次;已更新过期注释 | app/(student)/home.tsx:96-99; app/daily.tsx |
| C7 ✅ catalog 续播定位(2026-07-12 补完课时内精确续播) | 全部课程/详情按 user_lesson_progress 显示续播 | course/[id].tsx 早有"继续学习/开始学习"定位到具体课时(按 study_records)。**2026-07-12 补上课时内精确续播**(决策149·表建了3周从未接线):`user_lesson_progress` 只做"闻思步文字滚动位置"(一课一行存不下多步骤,决策149原注释即"继续阅读"取向)——离开 lesson/[id].tsx 页面时存当前闻思步滚动百分比,下次进同一课自动滚回原位;音视频进度/其它步不记(范围收窄,未做)。已用 hf 真实登录 REST 全链路验证:无记录→upsert建行→读回值一致→二次upsert原地更新(非重复行)→清理,测后无残留。⚠️ 未做真机验证(RN ScrollView 实际滚动行为需真机/模拟器,本环境无) | lib/queries/courses.ts(useLessonProgress); lib/mutations/study.ts(saveLessonProgress); app/lesson/[id].tsx |
| C8 ✅ **观修座次持久化**(2026-07-10 完成) | 观修计时/座次上报落库 meditation_sessions | 新表 `meditation_sessions`(RLS 同 daily_rituals/user_lesson_progress 先例)。⚠️ 施工中核实清楚一个关键边界:本表**只做个人历史记录**,不接入 practice_logs/愿状态机——大纲"≥3座×≥30分钟"升学硬条件仍只按「修持」页具体愿(如上师瑜伽/92修法)记录,两套不混;GuanStep"完成并计时"落此表,"累计"栏改读真实历史(此前刷新/离开页面即清零)。已在 sss-dev 用 hf 真实登录实测:插 35分/10分两笔 → 读回按口径算 sessionCount=1(仅≥30分钟计)、totalMinutes=45(全累计),与设计一致,测后清理 | supabase/migrations/20260710000400_meditation_sessions.sql; components/guan-step.tsx; lib/queries/meditation.ts; lib/mutations/meditation.ts |
| C9 ✅ **每日发心/回向落库**(2026-07-10 完成) | 三殊胜发心/回向仪式书签落库 | 新表 `daily_rituals`(user_id, ritual_date 主键;RLS 同 user_lesson_progress 先例:自己读写、admin 兜底读),`useTodayRitual`/`useMarkRitual` 接线 daily.tsx;upsert 只带当次那一列,同日另一仪式不被清空。本地一次性 SQL harness 验证迁移+RLS(316 断言全绿,新增4条);sss-dev 由 PM 经 dashboard SQL Editor 手动应用(本 chat 会话 Supabase 连接器未生效),已用 hf 真实登录 REST 实测 upsert 合并行为(先发心后回向,前者不被清空)+ RLS 读写全链路,测后清理;`check_schema_parity.py` 确认线上表已在场、无漂移 | supabase/migrations/20260710000300_daily_rituals.sql; lib/queries/daily.ts; lib/mutations/daily.ts; app/daily.tsx |
| C10 ✅ lesson 无障碍+补录日期已接 · 圆满自动判定已提前到 v1.0(见 F7) | a11y 取 profiles.accessibility_needs;圆满 v1.5 自动算;补录日期选择器 | ⚠️ 2026-07-11 核实纠正:此前记成"3件事全TODO",实查 a11y(a11yNeeds/a11yProfile)和补录日期选择器(backdate)其实早就接好了,注释过期未同步;真正还没做、且明确排到v1.5的只剩"study_records圆满标记自动算"。⚠️ **2026-07-12 订正引用**:此前(含代码注释 lesson/[id].tsx:36)一直引"决策46/103",核对 design_v2_decisions.md 原文发现两条决策实际内容均与此无关(046=关怀页联系方式展示、103=觉学对齐红线批),系误引——真正出处是**延后-4**(闻思圆满自动判定算法,原设计推v1.5+,维持推后;v1.0数据已记录,人工判)+**决策017**(升学/留级/毕业人工判定),代码注释已同步订正。✅ **2026-07-12 延后-4本体已提前完成**:PM 拍板"课程圆满=闻思+功课两者都要"后,自动判定已做成 DB 视图并接入管理端,见下方 **F7** | app/lesson/[id].tsx:93-95,151-159,482+ |
| C11 ✅ 藏历数据源(2026-07-09 完成) | ~~改走 tibetan_days~~ 已改走真实两表 tibetan_calendar+buddhist_days,本地 JSON 兜底 | 注意:两表同样仅 2026 全年,**跨年前需数据侧续灌 2027**(这是数据任务非代码任务) | lib/queries/tibetan-db.ts |
| C12 ✅ 决策已定:复习(SM-2)功能延后(2026-07-11) | 决策142/152/163 定复习中心 4 mode(今日待复习/错题本/收藏题/随课复习)+ SM-2 算法 + 3档自评(忘记/犹豫/记住);首页 3 号卡位、daily.tsx"复习"步均未实现 | 施工前排查真实范围时发现:①`question_responses.is_correct` 现从未被任何前端写入,是判分的真实缺口;②"收藏题"需要新表(库里现无此概念),属新 DB 改动;③首页实际 3 号卡位现由「大学演讲」占,并非决策163 原定版式的「复习」(home.tsx 顶部注释仍留"复习"字样,是已知漂移)。PM 2026-07-11 拍板:**整体延后**,不在本轮做。home.tsx/daily.tsx 相关注释已标注延后,防止后续误判为遗漏 | app/(student)/home.tsx:24-26,111; app/daily.tsx:18-19; supabase/migrations/20260618000040_questions.sql(sm2_cards 表结构已在,零消费) |

## D. 管理端内容编辑（2026-07-11 PM 拍板：编辑完全由官网/ETL 做，App 端维持只读，不再受开关待定）

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| D1 ✅ 决策已定:App 不做内容编辑(2026-07-11) | ~~表单 submit 接 insert~~ | PM 拍板:课程/读物的新建与编辑完全由官网/ETL 线做,App 端维持只读消费。已删掉 CONTENT_EDIT_UI 开关本身及其守护的全部死代码(新建课程/读物表单、节次/文章编辑按钮),不留孤儿代码(三易·易维护) | app/(admin)/course-content/index.tsx; [courseId].tsx; lesson/[lessonId].tsx; book/[bookId].tsx |
| D2 ✅ 读物文章"查看"已接(2026-07-10)· 编辑同 D1 决定不做 | 文章「查看」接详情/编辑页 | "查看"复用师兄端 /speech/[id] 读同一份 self_study_blocks;"编辑"随 D1 一并删除(不做 App 端内容编辑) | app/(admin)/course-content/book/[bookId].tsx |
| D3 ✅ 班级共修(2026-07-11 补完)· 全班圆满数/出勤率待教务定口径 | 无班分支「即将开放」预告 → 真班级共修;全班念诵总和/圆满数聚合 | ⚠️ 2026-07-10 核实:注释列了 4 项"待接(复杂聚合)",实查代码 2 项其实已接——全班念诵总和(`useCohortWeekTotals`已渲染)、班级愿(`classVows`已渲染);真正卡住的只有「全班圆满数/出勤率」,且**代码自己已标注为"待教务口径"**(classes.ts:11,502——不是没做,是业务标准没定,不该猜)。✅ **2026-07-11 补完最后一小项**:"本人本周已学进度"已接——新建 `useLessonStatusMap`(lib/queries/courses.ts),按节次列表批量取(不预设单一课程,防书衔接周横跨2门课漏判);视觉抽为共享组件 `components/lesson-status-badge.tsx`(`LessonStatusBadge`),course/[id].tsx 与 class.tsx 现共用同一份、不重复定义。已用 hf 真实登录 REST 核对全部 5 条子查询(course_lessons+courses 内嵌/profiles/study_records/personal_study_records/questions)语法与权限均正确。⚠️ **2026-07-12 判定口径已换**:原引用的"单一真源 `isLessonComplete`" 2026-07-12 起已删除(其盲聋判定偏松,详见 F7),`useLessonStatusMap` 改直接查 `v_lesson_completion` 视图,行为不变(仍是同一套"听/读/答→complete/partial/未学"三态),只是判定规则更准 | app/(student)/class.tsx; lib/queries/courses.ts; components/lesson-status-badge.tsx; app/course/[id].tsx |

## E. 待教务/外部数据（UI 已成型·等数据源）

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| E1 ⏳ slide-viewer 课件图(2026-07-10 接线完成) | 接 lesson_resources 的 slide_image_urls(非 practice_guides——那张表0处消费、字段也不对口,真实链路是观修资源走的 lesson_resources) | ✅ 前端已全部接通:观修「修」步(components/guan-step.tsx)有图片优先用 SlideViewer 幻灯浏览、无图退回原下载按钮;`slide_image_urls text[]` 列已加(20260710000200)。⏳ 仍等运营/ETL 导出课件图片回填该列,回填前显示原下载按钮兜底(不影响可用性)。✅ **2026-07-12 补**:SlideViewer 现已支持离线下载缓存(见 F6),回填前的原下载按钮兜底分支明确不做离线(见 F6 备注) | components/guan-step.tsx; app/lesson/[id].tsx; lib/queries/courses.ts |
| E2 传承要求配置(⏳ 待v1.5/教务)· 成绩录入(✅ 早已完成,原记录过期) | 传承要求配置 + 线下考试成绩录入 | ⚠️ 2026-07-11 一致性调研纠正:此前记录只看了 line 265 那句空状态文案「成绩尚未录入」,没看到其下方(267-270行)一个完整可用的「录入成绩」/「再录一次」按钮,弹出 ExamModal(姓名必填+分数0-100校验),接 useRecordExamGrade 真实写入 exam_grades——这部分早就做完了,只是文案容易让人误判。真正还开着的只有传承要求配置(line 221「本专业暂未配置传承要求」,完全没有后台UI能设置一个专业需要几项传承,依赖 v_advancement_5dim 视图) | app/(admin)/advancement/[studentId].tsx:221(待做) / 265-270(成绩录入已做) |
| E3 ⏳ 隐私与条款定稿 | 学会/法务定稿替换 | 占位文本、更新日期「待定」(上架前必替换) | app/legal.tsx:9 |

## F. 弱网 / 离线能力(2026-07-12 PM 提出「缺省页/后台完善度/弱网/离线」一并排查·6 阶段全部完成)

| # | 要做 | 现状 | 出处 |
|---|---|---|---|
| F1 ✅ 学修数量弱网写入幂等性 | practice_logs 弱网下点「记录」可能超时重试,原实现无去重,有漏记/重记双向风险 | client_token 幂等方案:客户端生成一次性 token,DB 加 partial unique index;冲突(23505)按成功处理(查回已有行 id),不算失败也不算新增。已用 sss-dev 真实数据实测:重复 token 二次提交,current_count 不重复累加 | supabase/migrations/20260712000000_practice_logs_idempotency.sql; components/quick-count-sheet.tsx; lib/mutations/practice.ts |
| F2 ✅ 弱网基础设施 | NetInfo 未接入 TanStack Query,onlineManager 原生端恒 true,networkMode/refetchOnReconnect 形同虚设 | 接 NetInfo→onlineManager(官方 RN 集成点);新增全局离线提示条(components/offline-banner.tsx,中性墨色非红色,守"无状态色"精神);mutation 默认仍不重试(多数是未做幂等的纯 INSERT,盲目重试会把"可能漏记"变成"可能重记"),只有已做幂等的(practice_logs)单独开 retry | lib/query-client.ts; lib/network-status.ts; components/offline-banner.tsx |
| F3 ✅ 后台管理 2 处修复 | 同 B3(见上) | — | 同 B3 |
| F4 ✅ 缺省页系统性扫尾(39 屏 + 3 处具体 bug) | 全仓排查"查询报错"与"数据为空/未找到"混为一谈的屏幕 | 36 屏补 loading/error/empty 三态(3 个并行 agent 分批查+我本人查 3 个具体逻辑 bug);排查中发现更深一层问题:12 个"按 id 查详情"的 hook 在 queryFn 内部把 `{data:null,error}` 統一按 `if(error||!row) return null` 处理,导致刚补的 isError 分支对 RLS 拒绝/后端报错这类场景形同虚设(只有断网等硬故障才触发)——一并修:12 处 hook 改 `error` 先 throw。CI 排查一次 local-stack-tests 偶发失败(④班级本周课→⑤课时学习 e2e 断言),复跑同 commit 即过,确认非本轮代码回归;顺手把 smoke.spec.ts 补上 boundary.spec.ts 早有的 `waitForTimeout(500)`(乐观写后查库的既有先例,此前唯独这条漏加)。✅ **2026-07-12 补第13处同类**:`useCurrentUser`(全站角色路由入口)当时不在那14个详情页范围内漏了,单独修——比详情页风险更大,两个消费方(`app/index.tsx`/`app/(admin)/_layout.tsx`)反向出错:前者失败当"未登出"处理会误弹登录页,后者失败时角色门判不出、停在默认admin全菜单;已各加 isError 分支 + 重试,扫过其余20个消费点确认无第三处同类"按me做门"逻辑 | 见 commit `44cb604`/`3fc6a0a9`/`75402d5`/`82be656` |
| F5 ✅ 离线内容第一层:浏览过自动持久化 | "浏览过的课程内容,下次没网也能直接打开",不需要手动下载 | `QueryClientProvider`→`PersistQueryClientProvider`(AsyncStorage,30天),白名单只放行课程内容类只读查询(course-detail/lesson-detail/lesson-questions/self-study/speech 等),进度类查询(会变、陈旧值可能让本人误判进度)不进白名单;根节点加 `useIsRestoring` 门,避免灌回完成前离线闪错误态 | lib/query-persist-allowlist.ts; lib/query-persister.ts; app/_layout.tsx |
| F6 ✅ 离线内容第二层:音频/观修课件主动下载 | 音频离线下载 + 观修 PPT 离线打开(决策089/080 原定 v1.0 范围,此前只有 Linking.openURL 外部打开、不算真离线) | expo-file-system 新版 API(SDK56)——官方明确不支持 web,故整个下载入口 `Platform.OS!=='web'` 才渲染;Zustand+persist 记下载清单元数据;AudioPlayer/SlideViewer 内部本地优先(有缓存播本地,下载完成自动切换,调用方无感);「设置→离线下载管理」新增管理页(列表/单删/清空/占用)。⚠️ 明确不做:观修课件"无逐页图、只有旧 downloadUrl 外链"那条兜底分支的离线支持(需要新引入文档查看器依赖,且该数据缺口本就在被 ETL 回填收窄,见 E1);视频(YouTube 内嵌)不在范围内。⚠️ 未做真机验证——expo-file-system/expo-av 是原生模块,本环境无 Docker/真机,只过了 tsc/jest/lint,下载/播放/删除实际手感需 PM 真机过一遍 | lib/downloads.ts; lib/downloads-hash.ts; lib/download-store.ts; components/download-button.tsx; components/audio-player.tsx; components/slide-viewer.tsx; app/downloads-manage.tsx; app/settings.tsx |
| F7 ✅ 课程圆满架构:闻思圆满+功课圆满(PM 2026-07-12"两者都要")+ 毕业/升学口径改按大纲绝对数 | "课程圆满"此前无自动判定(延后-4,v1.5+人工看原始数据);毕业/升学量化维度用的是运营占位比例阈值(出勤85/70%、修量近似倍率、考试60/80分固定线),与大纲原文绝对数规则不符 | 详见 `docs/待回写中枢_2026-07-08.md` D3。4 张新视图(`20260712000200_completion_views.sql`,均 security_invoker):`v_lesson_completion`(单课·听/看次数+读+答思考题,盲聋按次数≥2)→`v_course_completion`(整本课·bool_and 汇总)→`v_program_practice_completion`(专业配置的有限终点功课模板明细,缺失愿按0进度、不悄悄漏判)→`v_program_practice_summary`(专业功课圆满汇总)。考试合格线按大纲真实分级规则自动算(`lib/exam-pass-line.ts`:出勤≥93次→30分;<93→开卷72/闭卷60分),`exam_grades` 加 `exam_format` 列,管理端录入弹窗加开卷/闭卷选择,不再人工勾选是否合格。`lib/admin-thresholds.ts` 删除两个占位常量(`PRACTICE_TARGET`/`EXAM`,大纲无此比例概念)。管理端「报数升学」详情页③修量新增按 `v_program_practice_completion` 的专业配置功课明细子列表(区别于原有"已发放的升学硬依据愿"列表,含尚未 provision 的模板)。本地 SQL harness 新增 `14_completion_views.sql`。✅ **2026-07-12 PM 已手动应用迁移到 sss-dev**(本会话 Supabase MCP 连接器不可用,PM 用 dashboard SQL editor 手动跑);已用 hf 真实登录 REST 核对:①4张视图 `check_schema_parity.py` 全部在场;②`v_program_practice_completion` 对真实种子数据(`种子·百字明10万`)返回 current_count 正确(hf 本人0/另一真实学员36000,均 is_complete=false 符合预期);③验证 admin 跨学员读取路径(hf 读了另一真实学员 user_id 的行,取到不同于本人的真实 current_count,确认 RLS admin 兜底读全体正常工作,这正是管理端详情页需要的路径)。

✅ **2026-07-12 追加:"接UI"——course/[id].tsx/class.tsx/dossier.ts 圆满判定已统一为新视图**(PM 问过"要不要顺便统一",选"统一(推荐)"):此前已知出入(新视图盲聋按次数≥2、既有 `isLessonComplete()` 只判断"听/看过没有"的布尔值)已解决——`isLessonComplete()` 整个删除(核对 `tests/casebook/counting.md` HQ-8 确认判例本就是"听/看2遍",旧函数是判定偏松的 bug,不是刻意设计),`useMyCourseLessonStatus`/`useLessonStatusMap`/`useMyStudyCompletion`(dossier.ts)三处消费方全部改查 `v_lesson_completion`。新增 `v_lesson_completion.touched` 列(迁移 `20260712000300_lesson_completion_touched.sql`,`CREATE OR REPLACE VIEW` 追加列,非破坏性)——is_complete 单独不够区分"未学"vs"进行中未达标"(视图对任何人任何节都有一行,未学的节 is_complete 天然是 false),touched 补上这个信号,UI 三态(圆满/进行中/未学)不变,只是判定更准。course/[id].tsx 新增"本书圆满 ✓"标(`useMyCourseCompletion`,按 `v_course_completion.is_complete`,不是靠 completeCount===总课时数推断——`course.total_lessons` 是运营手填静态列可能失修,视图用真实 course_lessons 行数更可靠)。dossier.ts 的 `useMyStudyCompletion` 顺带大幅简化(原本地重算三次 study_records/personal_study_records/questions 查询 + listen/read/touch 集合运算,现单条查 `v_lesson_completion` 即可)。本地 SQL harness 补 2 条 touched 断言(TC-1/TC-2),全量 345 断言无回归;tsc/jest/lint 全过 | supabase/migrations/20260712000200_completion_views.sql, 20260712000300_lesson_completion_touched.sql; supabase/tests/14_completion_views.sql; lib/exam-pass-line.ts; lib/queries/advancement.ts(useProgramPracticeCompletion); lib/queries/courses.ts; lib/queries/dossier.ts; app/(admin)/advancement/[studentId].tsx; app/course/[id].tsx |

---

## ⚡ 排期更新（2026-07-08 PM 逐条裁决·详见《测试缺口与后台映射》裁决节）
- **新增近期大项**:代行三类 UI(替代/追认/豁免) · 传承/灌顶录入 · 讲考安排与记录 · 藏历月历编辑+年视图+JSON 导入导出(口径澄清=按决策137/172/174 补) · 画报法会期/特别日 · admin 全平台共修场次。
- **新增小活**:代暂停 auto 愿 · 报数一键复制 · 无障碍管理端展示+代登记 · 审批门补审计 · E2E testID 地基(lib/testids.ts+5 流程)。
- **A1 自选经选择器扩波**:与后台清单管理 + 座次门槛 min_session_minutes 配置同波(功课配置域一次做全)。
- **先建库**:reminder_presets CRUD(投递方式随决策188)。平台公告/推送规则/推送三点随通知链。
- **排期不急**:题库审核队列 · 专业纠错(移班顶) · Excel 导入(保留待办)。

## 🎨 交互设计逐条拍板（2026-07-08 PM·11 条）
| # | 项 | 设计裁决 | 状态 |
|---|---|---|---|
| ① | 代行三类 | **学员详情页统一入口**:动作面板加「代行」→弹层选类型(替代/追认/豁免)+对象(愿/课/考试/升学/其它)+必填理由,写 proxy_action_records | ✅ 已完成(波B) |
| ② | 传承/灌顶录入 | **学员详情页**独立「传承记录」区:仅显宗(课程/法会所得)录入,写 user_transmissions(source=proxy_recognize)。⚠️ **灌顶不做**——施工中发现与决策060/112(红线决定:灌顶记录砍·本库0痕迹)冲突,已请示 PM 维持红线(不因决策173解除"密法0痕迹"否决权而反推,173与060/112是两个独立决定) | ✅ 传承已完成(波B)·灌顶维持不做 |
| ③ | 讲考 | **并入出勤区**:attendance 改「共修与讲考」双 tab,建场次→逐人记三态+等级(speaking_sessions 表已建) | ✅ 已完成(波D);✅ **2026-07-10 补完**:讲考挂共修场次(决策080)选择器三处(新建表单/清单/详情)已接线——新建可选挂靠、清单卡片显示挂靠状态、详情页可改挂/取消挂靠(均走 speaking_sessions_write RLS·zhumai+admin),留空仍=独立记录(决策080合法态);已用 REST 实测建测试数据全链路走一遍+还原,不留痕 | app/(admin)/attendance/index.tsx; app/(admin)/attendance/speaking/[id].tsx; components/new-speaking-form.tsx |
| ④ | 自由文本加功课 | **移除**:加功课一律从 practices 修法库选(含自选经清单);新修法找 admin 加(数据可聚合·三易) | ✅ 已完成(波C·Part3) |
| ⑤ | 座次门槛下限 | **UI 硬限 ≥30 + DB CHECK 收紧 >0→≥30**(查文档定:大纲行105 底线+PD-2 要点③) | ✅ 已完成(波A·DB侧 + 波C·Part1·admin配置UI) |
| ⑥ | 藏历 | **App 不做编辑 UI**——直读共享库,替换本地 JSON | ~~✅ 已完成(波A)~~ **✕ 原实现查的 tibetan_days 是已废弃、从未建成的表(404),切源从未生效**;**✅ 2026-07-09 重修**:改查真实两表 tibetan_calendar+buddhist_days(2026-06-19 定案·365+749行),三消费点实测通过;auspicious🌺 判定规则不明暂恒 false(见 commit b10f30d 后续) |
| ⑦ | 画报法会期/特别日 | **扩 home_posters**:加 poster_type+start/end 日期 | ✅ schema已完成(波A);✅ **2026-07-11 后台UI+首页替代逻辑补完**:后台"藏历画报"页加"法会期画报"/"特别日画报"2 个新 tab(列表+新增/编辑/删除,不同月度那种固定12格);首页 `useCurrentPoster`(lib/queries/home.ts)此前只读月度,现补齐 special>event>monthly 优先级替代查询(PM 2026-07-11 拍板:法会期间【替代】而非叠加当月画报);顺手订正该函数"今天"取值(此前用设备本地 `new Date()`,改用 `todayUTC8()`,同藏历一套口径)。已用 hf 真实登录 REST 插入 event+special 两条覆盖当天的测试画报,核对替代优先级二者均命中时 special 胜出,测后清理(含之前漏测的编辑/UPDATE 路径,一致性检查时补测)。✅ **同日再打磨 3 项**(PM 确认按建议做,存储孤儿文件清理项 PM 明确判定"App 域内自由编辑、非跨域问题"故不做):①编辑弹层日期重叠时黄色警示(非阻断,复用已加载的列表数据不额外请求)②列表状态标记从静态"启用/停用"改"今天生效中/已启用非当前日期/已停用"③删除确认对"今天生效中"项加特别提醒(说明删除后首页会退回下一优先级)。重叠/生效判定用简单日期字符串比较(闭区间),已用 node 脚本核对 10 条边界用例(含首尾相接、互相包含、跨界) | lib/queries/home.ts; lib/queries/posters.ts; lib/mutations/posters.ts; app/(admin)/poster-calendar/index.tsx |
| ⑧ | 平台共修场次 | **group_sessions.cohort_id 改可空** | ✅ schema+RLS已完成(波A) |
| ⑨ | 报数一键复制 | **默认模板先上** | ✅ 已完成(小活·决策064/074a:班级详情页「本周报数」按钮,只出聚合总量·#193) |
| ⑩ | 提醒语预设库 | **独立路由**(改口:非 practice-config tab,建 app/(admin)/reminder-presets/) | ✅ 已完成(波C·Part4) |
| 小活 | 代暂停/无障碍/审批审计 | 代暂停auto愿(决策071)+无障碍管理端展示代登记(决策078)+审批门补audit_logs+按注册意愿(learning_mode)自动授予self_study_grants | ✅ 已完成(波B) |
| 顺序 | 施工波次 | 波A ✅ → 波B ✅(学员详情面板) → 波C ✅ 功课配置域(Part1门槛/Part2清单管理/Part3学员选择器/Part4提醒语库全完成) → 波D ✅ 出勤区讲考 → **小活 ✅**(报数一键复制+E2E testID地基) | **前端交互设计11条全部完成** |

## 备注（非 UI，但与 UI 联动的后端前置）
- **SD-4 Edge cron 落 cohort_lag_snapshot 快照** 未建——⚠️ 2026-07-11 核实纠正:"supabase 无 functions 目录"已过时(C4 账号注销已建 `supabase/functions/purge-deleted-accounts/`),但 cohort_lag_snapshot 专用的这一个 cron 函数确实仍未建;A3/B1 的"历史快照"依赖它,v1.0 靠读时算顶,不影响可用。
- **SD-6 formal_since 转正列** 缺失——出勤维「只算正式期」无法切,影响 B1/E2 出勤口径。
- **听课维/答题维** 无数据流,get_care_dims 恒 na——影响 B1 5维完整度。
- 上述 3 项属后端工程,非纯 UI,但相关 UI 完整呈现依赖它们。
