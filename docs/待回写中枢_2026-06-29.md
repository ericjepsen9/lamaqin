# 待回写规划中枢清单(2026-06-29)

> 性质:**App 线施工产出**,待 PM 同步回中枢 `sss/Planning/` 对应文件,再按**文档分发方案A 单向覆盖**回本 repo 的 `docs/` + `CLAUDE.md`(防分叉)。
> 接力协议(CLAUDE.md 规定):改中枢共享文档**前**先读 `决策定稿_课程专业数据模型` 落地段;改**后**在该文件追加一行(日期 + 改了啥)。
> 三类分开放:**A 工程规约→中枢 CLAUDE.md** / **B 产品决策落地→决策文档** / **C schema 增补→schema_phase1**。

---

## A. 工程/施工规约(建议加入中枢 `CLAUDE.md` 新增一节「§ 施工规约」)

> 草稿文本,可直接粘贴:

```
## N · 施工规约(Claude 每会话遵守)
1. **DB 改动**:用 Supabase MCP `apply_migration`(进 supabase_migrations 台账),勿用裸 execute_sql 改结构;迁移文件带登记 banner(源/应用矩阵/依赖);改完用 scripts/check_schema_parity.py 校验"仓库=库"无漂移。开发只连 dev,不连生产。
2. **逻辑落点**(定一处,别满世界找):数据派生聚合→DB 触发器(原子);连续重算状态→Edge cron;业务动作(发愿/判定/审批)→**应用层显式调用**(失败要可见,禁在触发器里吞异常);跨表读聚合→视图(security_invoker)。
3. **类型**:`lib/types/database.ts` 由 `scripts/gen-types-from-migrations.cjs` 从迁移生成(决策175),改迁移后重跑;**勿手改**——生成器漏(ALTER 加列 / 视图列)就补生成器,别改输出文件。
4. **量化门槛**:出勤率/修量/升学等未经教务定的阈值,放 `lib/admin-thresholds.ts` 并标「占位·待核」,勿散落各页冒充定论。
5. **验证**:关键业务逻辑须 PM 人工验(报"测试通过"≠语义正确);Claude 用 REST(以师兄/admin 真身走 RLS)或 MCP 实测写路径,测后还原数据。
6. **种子数据**:仅 sss-dev(标记 @seed.bicwny.test / SEED_ / 种子·),生产绝不灌;上线前清(清除 SQL 见施工记录)。
```

---

## B. 产品决策落地(追加到中枢 `design_v2_decisions` / 对应决策文档)

- **决策012 落地**:功课模板挂 **program(专业)**(`practice_templates.applies_to_programs`),cohort 可覆盖(`cohort_recommended_templates`);发愿**逐「修法」fallback**(本班覆盖→专业默认)。班级:加学员/入班时**应用层** `provision_cohort_vows` 实例化;自学:按 program 默认。**愿不挂在 `courses`(单本课程书)/课时上**,只在专业层与课程交汇(决策160:闻思与修两套不混)。
- **内加行限时落地(073/086/122)**:`practice_templates.is_time_limited`;限时愿起修=开班日+`starts_offset_days`(梯次)、截止=起修+`cohorts.neijiaxing_lock_years`(默认4)、仅 admin 延期/过期锁定。
- **升学5维落地(017/123/124)**:`v_advancement_5dim` 聚合 4 量化维(传承/出勤/修量/考试),**发心人工不入**;判定=**人工·不 auto-gate**,写 `advancement_records` 留痕 + 改 `class_members.status`;考试成绩 `exam_grades` 后台录入(决策125)。
- **关怀落地(107/035/044-046)**:管理端 5 维滞后;v1.0 **在线算**(出勤率 + 修量进度=真实;听课/答题/观修=未接入,待数据流);阈值见 `lib/admin-thresholds.ts`·**待教务定**(决策延后-5);`care_followups` 写仅本班 zhumai/aixin(admin 只读,care_worker_id=本人);师兄端零呈现。
- **自学限时年限**:自学无班级配置 → 暂默认 4 年;**待 PM 复核**(纯自学内加行属边缘场景)。
- **关怀/升学列表**:列表行按 `class_members` 各行(兼修=多行),详情/锚定按最初正式班(决策123)。

---

## C. schema_phase1 增补(App 主写;已应用到 sss-dev,prod 上线需同样应用)

- `practice_templates` **+`is_time_limited boolean NOT NULL DEFAULT false`**(决策086:内加行从硬编码降为模板数据实例)。
- 新增函数(SECURITY DEFINER,迁移 `20260628000020_provision_cohort_vows.sql`):
  - `provision_member_vows(user, cohort)` / `provision_selfstudy_vows(user, program)`(后者带本人/admin 闸)/ `provision_cohort_vows(cohort)`(admin/本班主麦)。
  - 绕过 `user_practice_vows` 的「仅本人插愿」RLS,供应用层显式发愿;不建触发器。
- 注:`cohort_recommended_templates` / `practices`(13 种子)/ `v_advancement_5dim` / `advancement_records` / `exam_grades` / `care_followups` 均已在基线;本轮新增仅上面一列 + 三函数。

---

## 备注:本轮已落地但**未触动事实源结构**的(无需回写,仅施工记录)
- 管理后台全模块接真实数据(总览/班级/出勤/功课配置/升学/关怀/法会/审计)。
- 类型生成器 `gen-types-from-migrations.cjs` 标识符正则修为认数字(修 `v_advancement_5dim` 截断 bug)。
- 漂移校验器 `scripts/check_schema_parity.py`、种子脚本/SQL(见 `施工记录_功课配置模块_2026-06-28.md`)。
