# 测试剩余工作 + 后台功能↔页面映射（2026-07-08）

> 来源:5-agent 工作流(woh9ies8v)盘点 + 对抗校验(可疑条目逐条回代码 grep 核实)。
> 基线:SQL harness 217 断言全绿(哨兵齐) · jest 18 用例 · 集成 1 件(JWT攻击) · E2E 零启动。
> 配套台账:《UI待设计_2026-07-08》(前端)·《待回写中枢_2026-07-08》(文档漂移)。

---

## ⚠️ 置顶:两条「裁决已下、实现+断言双缺」的红线（比补测试优先）

| # | 红线 | 现状 | 风险 |
|---|---|---|---|
| R1 | **净土三选一目标锁定(PD-6/PD-9)未施工** | 判例已裁(2026-07-06:DB 层锁死+admin 审批后门;目标只能是 5000/7500/900)——但库里**无任何白名单约束/触发器**(核实:迁移无三选一约束),`daily_target` 仅 CHECK>0 | 师兄本人现在就能把每日目标从 5000 改成 900 = **变相换号**,锁定规则形同虚设 |
| R2 | **内加行过期补录锁定(HQ-4/#190/PD-26)未实现** | 「信任师兄全面开放补录」的唯一例外=内加行过期锁定,应用层未实现、零断言 | **91 位老学员植入(决策076)的硬前置**——不先做,老学员数据一进来过期锁定就是空的 |

> 这两条与 care.ts 是同一种病:判例/决策裁了、casebook 标了"✅ 已裁决",但没人施工。属计算层(约束+触发器+断言),判例先行适用,可自主施工后请 PM 验。

> ⚠️ **审计追记(2026-07-09,五路核查)**:R1/R2 的"已施工"要打折——① **整批 07-04~07-09 迁移(含 R1/R2)从未应用到 sss-dev**(仅存在于仓库,harness 绿=本地临时库绿≠dev 已上;待应用 SQL 包已交 PM);② R1 尾部防御性 UPDATE 猜的四个修法名全不中,真名=「阿弥陀佛号」(template_name≠practices.name),已出修正迁移 20260709000300(实名配置+白名单校验窄化+报错文案去内部编号);③ R2 被拒时师兄端快速计数入口原会**假成功**(无 onError 且无条件显示✓),已修(QuickCountSheet 等待写库结果+撤销接真删)。
>
> ⚠️ **审计追记(2026-07-10)**:PM 已把待应用 SQL 包跑进 sss-dev,parity 校验器重跑确认 15 项缺失清零(过程中自查发现校验器本身 4 处探测错——2处探错对象名(真产物是视图非函数)、2处探错参数名——已订正,非迁移问题)。跑完后逐页真机核验又挖到 4 个与本次迁移**无关的独立老 bug**,均已修复验证:① `practice_templates`/`class_members` 两表对目标表各有 2 条外键,代码裸嵌入(如 `profiles(full_name)`)触发 PostgREST 歧义拒绝(PGRST201)——功课配置"默认功课"板块、关怀清单列表+详情页因此报错,**此前一直被"列/函数缺失"的报错挡在更前面,从未真正暴露过**;② 全仓库排查同类风险另发现 `class_admins`(1处)、`class_members` 另外 4 处同款裸嵌入,一并修复(commit `d9a404b`);③ `useCurrentUser` 角色判定两条查询不查 error,失败会把真管理员静默判成 student、被角色门踢出后台;④ 留级次数(`held_back_count`)读失败会静默清零重记成1。四条均已 tsc+jest+REST/真机验证。
>
> ⚠️ **审计追记(2026-07-10 第二轮)**:①**R1 补一处遗漏**——DB 后门(`vows_check_daily_target` 触发器允许 admin 纠错+自动留审计)早就实现且已测(`03_constraints.sql`),但管理端从没接前端 UI,唯一能触发后门的方式是直连数据库;已补(`components/admin/student-actions.tsx` 学员详情"设宽限/暂停/纠错"卡片,选项限定白名单值、不给自由输入)。②**独立安全发现**:核实"App 对官网数据有没有写权限"时发现 `texts`/`dharma_assemblies` 两表写策略是 `USING(true)`——任何登录用户(非仅admin)理论上都能改这两张内容表,已收紧为 `is_system_admin()`(20260710000100);全仓库 0 处代码实际读写这两表,是沉睡未被利用的口子。③**Web E2E + JWT 集成测试接入 CI**:两套测试(2026-07-09 建)此前都只能本地手动跑,新增 `local-stack-tests.yml` 起本地 Docker Supabase 栈自动跑两者。④**JWT 测试补正向路径**:此前只有跨用户攻击负例,补了"合法自写应成功""爱心合法读本班关怀记录应放行"两条正向断言,过程中发现并修正了自己写的一处测试 bug(`chk()` 计数器被内层事务回滚吞掉,NOTICE 打印了"ok"但计数器没加,是断言层面的假绿,非业务代码问题)。⑤**观修课件图片幻灯接线**:`components/slide-viewer.tsx` 组件此前零处调用;查明真实数据链路是 `lesson_resources`(非 practice_guides)后加列 `slide_image_urls` 并接入 `guan-step.tsx`,有图用幻灯、无图退回原下载按钮,仍等运营/ETL 出图。SQL harness 由 309→312 断言,全绿。

---

## 一、测试剩余工作（29 条·按层/优先级）

### 高优先级（12 条）
| 层 | 缺口 | 说明 |
|---|---|---|
| 红线 | R1 净土目标锁定 | 见置顶 |
| 红线 | R2 内加行过期锁定 | 见置顶 |
| DB | ~~provision 三发愿函数零断言~~ **✅ 已补(10_provision_vows·16断言·含anon穿透缝修复)** | 迁移20260708000500 REVOKE PUBLIC/anon;闸/fallback/幂等/日期/自学全测 |
| DB | ~~三条核心写 RPC 零断言~~ **✅ 已补(11_write_rpcs·18断言)** | 守卫/扇出/personal路径/分项upsert/不降级/started_at回拨全测 |
| DB+单元 | ~~升学聚合两层全裸~~ **部分✅:视图已补(12_advancement_5dim·13断言)+修量口径收敛为只计必修(迁移20260708000500)** | App 聚合(advancement.ts)单测仍待补 |
| 单元 | ~~date-tz.ts 零测试~~ **✅ 已补(8用例)** | |
| 单元 | ~~vowPace 零测试~~ **✅ 已补(抽 vow-pace.ts·13用例)** | |
| 单元 | ~~speechCompleteDerive 零测试~~ **✅ 已补(抽 speech-complete.ts·11用例)** | |
| 单元 | ~~班级周数公式零测试~~ **✅ 已补(抽 cohort-week-calc.ts·6用例)** | 双写漂移有护栏 |
| E2E | ~~整层零启动~~ **✅ testID地基已补**(lib/testids.ts单一真源+5条核心流程埋点:OTP登录/首页/修持打卡/班级本周课/课时学习) | 决策4=iOS Maestro 先行;.maestro脚本+EAS构建仍留给上架冲刺前集中做(时机未到,非遗漏) |
| CI | JWT 集成测试不在任何 workflow | 回归零保障(需本地栈,可做专门 workflow 或 nightly) |
| 集成 | 写路径正向集成零覆盖 | 现仅攻击负例;真实 PostgREST+JWT 的打卡写路径(正是暴露"表级 GRANT 缺失"类问题的层)没测 |

### 中优先级（10 条）
RP-6/RP-7 判例标了没断言 · update_cosession_settings+两个审计触发器零断言 · get_current_week_lessons 组合函数零断言(与 jest 纯函数平行实现) · JX-6 编辑/删打卡减向路径 · HQ-9 审核态改删 RLS 分支 · JX-3 总修持时间全额累计 · 出勤 85/70 档位边界+双写护栏 · XJ-9 抄拜经 custom 累计专项 · question-card norm() 判分归一化 · serializeAnswer/mergeProgress/parseDims 四纯函数 · 组件层 18 个组件仅 1 件 smoke。

### 低优先级（7 条）
quiz stripNum/bookTitle · v_public_* 7 视图匿名面 · 已测模块残留边界(JX-4/8 等) · SS-5/6/7 app 查询层 · lint 18 处转硬门禁 · 代行影响语义(待功能落地·v1.5+)。

**判例覆盖总评**:已落地的算账核心(counting 51/state-machine 23/report-agg 7)覆盖扎实;缺口集中在**写路径 RPC、发愿链、升学聚合**三块 + **App 单元层纯函数几乎空转** + **E2E/集成整层未启动**。

---

## 二、确定的后台功能 ↔ 管理端页面映射（39 项核实）

### ✅ 有页面·真操作（核心运营闭环全线通,17 项）
审批→转正(发学号)→切主班→入班自动发愿→排课(幂等生成/自学/放假)→出勤录入(点名/补录/考勤开关)→关怀跟进(zhumai/aixin 写)→升学留痕(毕业/留级/离班+advancement_records)→考试成绩录入→宽限 due_date(带审计)→班级三层 CRUD(时区/休息周/归档)→任辅导员爱心→法会(场次/默认功课/公告)→题库(提取/建改删/参考答案)→自学特权授予→专业默认节奏→审计查看→反馈处理→Dashboard 待办。

### 🟡 有页面但缺角（8 项)
| 功能 | 缺什么 |
|---|---|
| 审批门 | ~~不写审计、不按注册意愿赋权~~ **✅ 已补(波B:approve/reject 写 audit_logs;learning_mode=self_study 自动授 self_study_grants)**;"不发学号"仍准确(学号=转正步骤发,非审批步骤) |
| 无障碍登记 | ~~没展示、不能代登记~~ **✅ 已补(波B:A11yChip 展示+admin 代登记,UI 门与 RLS 同判据)** |
| 功课配置 | 模板/覆盖/发放真;✅ 自选经清单管理 UI + 座次门槛配置入口已完成(波C·Part1/2/3);缺:改模板同步到已发愿(批量) |
| 题库 | 缺审核队列(zhumai 出题→admin 批/驳) |
| 排课/共修 | 缺推送三点接线、admin 全平台场次(决策066) |
| 画报 | 月度真;法会期/特别日两维度未做 |
| 公告 | 班级公告真;**平台公告+推送规则无页面** |
| 传承管理 | ✅ 显宗传承录入 UI 已完成(波B);灌顶按决策060/112(红线:0痕迹)维持不做——D13 升密硬条件的灌顶部分现在没法录,红线明知有此代价 |

### 🔴 无页面（确定功能·8 项)
| 功能 | 依据 | 备注 |
|---|---|---|
| ~~代行三类(替代/追溯认可/豁免)~~ **✅ 已完成(波B·d75cda3;本行原漏更·2026-07-09 审计勾正)** | 决策121 v1.0 | student-actions.tsx「代行」弹层→proxy_action_records;写权=admin/本班zhumai(RLS) |
| **讲考安排与记录** | A8 三态+等级 | ✅ 已完成(波D):共修与讲考双tab,三态(主讲/提问/听讲)+等级评价(选填·仅管理端);挂共修场次(决策080)**2026-07-10 补完接线**(见UI待设计③),schema先行阶段已收口 |
| ~~代暂停 auto 必修愿~~ **✅ 已完成(波B·d75cda3;本行原漏更·2026-07-09 审计勾正)** | 决策071 | usePauseVow/useResumeVow·student-actions 暂停/恢复钮 |
| 专业纠错(第一学期改 program) | — | 成员 program 无变更操作 |
| 提醒语预设库 reminder_presets | 决策130/102 v1.0 | ✅ 已完成(波C·Part4·独立路由CRUD);投递方式(推送A/B)待决策188 |
| ~~报数查看/分享(WhatsApp 复制)~~ **✅ 已完成(小活·5f5461e;本行原漏更·2026-07-09 审计勾正)** | A7 | 班级详情「本周报数·一键复制」 |
| Excel 排表导入(M4) | — | "一句话排课"已是替代路径,**建议 PM 确认是否可废** |
| 91 老学员植入工具 | 决策076 | 一次性上线动作;前置=R2 |

### ⏸ 空置但与决策一致（3 项)
学习提醒设置(决策188 投递方式 A/B 待 PM 选)·数据分析(决策199 暂缓)·LLM 管理台(非 v1.0)。

### ❓ 口径冲突需 PM 澄清（1 项)
**藏历维护**:页面自注"v1 只读查阅",但决策137/172/174 认定"月历编辑+年视图+JSON 导入导出"为确定后台功能——哪个是 v1 范围?

---

## ✅ PM 逐条裁决（2026-07-08·20 条全部拍板）

### 红线 ✅ 已施工（2026-07-08 同日·迁移 20260708000300/000400·03+07 断言全绿·待 PM 真机验）
- **R1 净土目标锁定 = 数据驱动白名单**:修法字典加「允许目标值」配置列,触发器校验(净土=5000/7500/900、心经=1~9)——一个机制同时落 R1 + PD-19 DB 层;admin 后门=写保护闸+审计。
- **R2 内加行过期锁定 = 立即施工**:限时愿过截止日拒新打卡/补录(按班级时区判今天)。91 老学员植入前置。

### 前端排期（按波次）
| 波 | 内容 |
|---|---|
| **近期·大项** | 代行**三类做全**(替代/追认/豁免·PM 加码) · 传承/灌顶录入 · **讲考**(PM 提前) · **藏历按决策补编辑**(月历编辑+年视图+JSON 导入导出·口径澄清=决策137/172/174 维持) · 画报法会期/特别日(PM 提前) · admin 全平台共修场次(决策066·先于推送) |
| **小活·顺手做** | ✅ 代暂停 auto 愿(071·波B) · ✅ 报数一键复制(A7·决策064/074a) · ✅ 无障碍管理端展示+代登记(波B) · ✅ 审批门补审计(波B) · ✅ **E2E 地基**(lib/testids.ts+5 核心流程埋 testID) |
| **同波绑定** | ✅ 自选经选择器(A1) + 后台清单管理 + 座次门槛 min_session_minutes 配置 + 提醒语预设库——功课配置域波C(Part1-4)全部做全 |
| **先建库后接** | reminder_presets 预设库 CRUD(投递方式=决策188 后接) |
| **随通知链** | 平台公告+推送规则+共修推送三点(依赖 notifications+决策188) |
| **排期(不急)** | 题库审核队列(等真有多人出题) · 专业纠错一键版(个案先移班顶) · Excel 排表导入(**保留待办**·问教务是否有历史表要迁) |
| **补全** | 审批门「按注册意愿赋权」(先核注册表单有无意愿字段,无则回 PM 定口径) |

### 语义裁决
- **改模板不自动追溯已发愿**(与 PD-2 快照语义一致);需要更新存量时 admin 重跑「同步发放」(幂等 RPC 已有)逐班覆盖,UI 标注清楚即可。
- **E2E 时机**:testID 地基现在埋,Maestro 脚本+EAS 构建上架冲刺前集中做。

### 测试块（Claude 自主补·无需再问）
DB 零断言块(provision 链/三写 RPC/升学视图/审计触发器等) · 单元纯函数(date-tz/vowPace/speechCompleteDerive/周数副本等) · 中低优先级清单按台账顺序。
