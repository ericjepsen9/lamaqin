# 测试计划·下一步(2026-07-16)

> **状态:规划中,按PM指示暂存本地,未commit/push。**
> 承接"系统性e2e测试补充"(83条测试,commit历史见`claude/focused-hamilton-000nc8`,§1-13批)之后的下一轮方向。本文件只是计划,不是已完成的工作——里面提到的测试都还没写。

## 0 · 优先级总览(PM可调整顺序)

| 优先级 | 维度 | 一句话 |
|---|---|---|
| **最高** | **⓪ 点击模拟正确性保障** | **现有83条测试从没真正跑过,先保障"模拟点击本身没问题"再谈别的维度** |
| 高 | ① 异常输入扩展 | 把`boundary.spec.ts`的模式搬到这轮新加的后台页面,性价比最高 |
| 高 | ② 权限边界·写层面 | 不只测"能不能进页面",测"没权限能不能绕过UI直接写库" |
| 中高 | ③ 时区边界 | e2e层面验证非Asia/Shanghai时区班级的"今日/本周"算得对不对 |
| 中 | ④ 并发/竞态 | 两人同时改同一条数据,后写会不会覆盖丢失 |
| 中 | ⑤ 数据量/规模 | 200人班级、500题课程,列表渲染不卡死 |
| 低 | ⑥ 键盘可达性 | 核心流程纯键盘(Tab+Enter)能否走完,配合A7一起做 |

---

## ⓪ 点击模拟正确性保障(最高优先,建议排在其它维度之前)

现状:这轮新加的83条测试(第1-13批,commit历史见`claude/focused-hamilton-000nc8`)全靠`tsc`/`lint`/`jest`/`playwright --list`四道静态门禁验证,**从没有在真实浏览器里被真正点过一次**。"模拟点击有没有问题"本身就是跑之前最该确认的一件事——如果testID对不上、文字定位有歧义、原生弹窗处理错了,第一轮真机跑出来的会是"测试代码自己的bug",不是"App的bug",浪费真机验证的第一次机会。

**两种做法,都写在这里、都还没执行,PM挑一种(或都要):**

**方案A · 跑之前先做一轮静态核对**(不需要真实环境,现在就能做):逐条测试把每个`.click()`/`.fill()`/`getByText()`/`getByTestId()`回源码核对——
- testID字符串是否真的打在对应组件的`testID={...}`属性上(不是改了组件代码但忘了同步测试,或者反过来)
- `getByText()`用的文字是否在当时的页面状态下**唯一**(常见坑:RN的`Modal`在web端不会把底层页面的DOM摘除,弹层内和弹层外可能同时存在同名文字/按钮,导致Playwright严格模式报错)
- 原生弹窗(`window.alert`/`window.confirm`,本项目`lib/dialog.ts`的`notify()`/`confirmAsync()`)有没有被误用`getByText()`去找——这个必须用`page.waitForEvent('dialog')`或`page.once('dialog',...)`接,`getByText()`永远找不到原生弹窗
- 多个原生弹窗连续出现的场景(先confirm、成功后再alert),第二个监听器有没有在accept第一个之前就注册好,避免漏接
- Expo Router的路由分组`(admin)`/`(auth)`在实际URL里会被去掉,`page.goto()`/`toHaveURL()`有没有不小心带了分组前缀
- 点击的元素在测试跑到那一步时,是否真的可点(没被`disabled`挡住、不是还没等前一步的查询刷新完就点下一步)

这项工作我可以做,但**只有你明确说"开始做"我才会真的动手**(上一次我把这个理解成"你已经让我去做了",提前发了3个后台agent去审查,你纠正了我——现在写清楚在这里,等你确认)。

**方案B · 直接跑,失败了再对照源码诊断**:不预先审查,直接在真实环境跑一次,失败的每一条按报错信息回源码定位问题、修复、重跑,直到3次连续绿。这样"审查"和"验证"是同一步,不用先花一轮时间做静态核对。缺点是如果testID层面的问题比较多,第一轮的失败列表可能会比较长、不容易一眼看出哪些是"测试代码写错"哪些是"App真的有bug"。

两种方案不互斥,常见组合是:方案A做一遍收敛掉明显的低级问题,方案B在真实环境里收口验证语义正确性。

---

## ① 异常输入扩展(高优先)

现状:`boundary.spec.ts`只测了学员端几个页面(数字填0、超大数字封顶、超阈值二次确认、日期格式错/填未来)。这轮新加的10个后台spec文件(attendance/advancement/scheduling/practice-config/quiz)几乎全是"正确输入→正确写库"的happy path,没测过异常输入。

具体目标(按文件):
- **`e2e/admin-advancement.spec.ts`**:考试成绩录入——分数填`-1`/`101`(超出0-100范围)、非数字字符、小数;考试名称留空能否提交。
- **`e2e/admin-scheduling.spec.ts`**:排课起止节号"从第N节到第M节"里`N>M`;每周节数填0(Stepper有min=1兜底,验证真到不了0);自学读物起止周填超大值(200+)。
- **`e2e/admin-practice-config.spec.ts`**:每日/每周目标填负数(TextInput的keyboardType=numeric,验证前端能不能真的挡住,不能只指望DB CHECK);每座门槛填29(低于30分钟大纲底线,已有部分覆盖,补更多边界如刚好30/31);完成天数/起修偏移天填负数。
- **`e2e/admin-quiz.spec.ts`**:题干/参考答案填超长字符串(测numberOfLines截断、保存后不崩溃,不是测DB有无长度限制);单选题选项只填1个(isPayloadComplete要求≥2,验证保存按钮确实被禁用)。
- **`e2e/admin-attendance.spec.ts`**:讲考等级评价在非主讲状态下尝试设置(验证UI是否正确隐藏该选项,而不是DB兜底)。

## ② 权限边界·写层面(高优先)

现状:`resilience.spec.ts`测的是"URL直达能不能看到页面"(UI导航层拦截)。这批要测的是更深一层——**没权限的角色能不能绕过UI直接触发写操作**,UI隐藏了按钮不代表RLS真的挡住了。

具体场景:
- 非本班辅导员:登录后直接改URL到别的班的`/classes/[id]`(如果能进去)尝试点名/发起排课变更 → 应被RLS拒(42501),前端应显示友好错误,不崩溃、不静默失败。
- 学员本人:尝试通过某种方式(如篡改请求)直接改自己的`class_members.status`(跳过admin判定接口)→ 应被RLS拒。
- 已毕业/已离班学员:登录后尝试补打卡 → 应被拒(任务清单里提到已经修过`已毕业/离班学员打卡未真正禁用`,这里是做回归验证,不是新发现问题)。
- 爱心(aixin)角色:尝试修改思考题参考答案(题库页面明确写"辅导员只读") → 应被拒。

方式:优先用真实登录的低权限账号(zhumai/aixin/student)在页面里尝试操作,断言操作失败+错误提示正确;比直接绕过UI伪造请求更贴近真实攻击面,也更符合TESTING.md"用真实登录路径"的既有测试哲学(同`tests/integration/`那层的做法)。

## ③ 时区边界(中高优先)

现状:`lib/queries/cohort-week-calc.test.ts`是纯函数单测(有DB侧交叉引用),没有e2e层面验证"UI显示"是否正确套用了对应时区。

具体场景:
- 建一个`timezone='America/Los_Angeles'`(或任意非`Asia/Shanghai`)的一次性测试班,验证:出勤页"本班当前·第N周"、报数弹层"今日"计数、`get_current_week_lessons`驱动的本周课程列表,是否按**该班时区**算,而不是测试机器/浏览器的本地时区。
- 藏历殊胜日固定UTC+8(决策075)——找一个已知的殊胜日,在临界时刻(如UTC 15:30-16:30,对应北京时间23:30-次日0:30这类跨天窗口)验证UI标记是否正确。这条如果测试环境时钟不可控,可能只能记录为"结构性难测",标注原因而非强行造一个假的system time hack。

## ④ 并发/竞态(中优先)

现状:这轮完全没碰。

具体场景:
- 两个浏览器上下文(两个辅导员)同时打开同一场共修的点名页,几乎同时提交 → 验证`study_records`的唯一索引(`uniq_group_attendance_per_lesson`)是否正确拦下重复/冲突写入,以及UI层面"后提交的会不会覆盖前面已保存的"这个语义(现在的实现是先查后写,存在TOCTOU窗口,值得验证实际行为而不是假设)。
- 两个admin同时打开同一个功课模板的编辑弹层,各自改不同字段(一个改名称、一个改目标数),先保存的会不会被后保存的整体覆盖(`useUpdateTemplate`是整行UPDATE,没有字段级合并,预期后者会覆盖前者已保存的改动——这条测试目的是**确认这个已知行为**,不是找bug,但值得让PM知道这个设计现状)。

## ⑤ 数据量/规模(中优先)

现状:这轮所有fixture都是1-3条数据规模,没测过真实体量。

具体场景:
- 造一个200人的一次性测试班,验证`classes/[id].tsx`的学员名单、`advancement/index.tsx`的报数升学列表渲染是否卡顿/是否需要虚拟滚动(如果现在没做虚拟滚动,这条测试的目的是**量化现状**,给PM一个"多少人开始明显变慢"的参考数字,而非假设一定要修)。
- 造一门500题的测试课程,验证`quiz/index.tsx`按节分组展开后的列表渲染。

## ⑥ 键盘可达性(低优先,可与A7无障碍工作合并)

现状:`accessibility.spec.ts`用`@axe-core/playwright`扫描了4条核心流程的无障碍标签,但没测纯键盘操作。

具体场景:
- 登录→首页→打卡这条最核心的路径,全程只用Tab切焦点+Enter/Space确认,不用鼠标点击,验证能否走完整个流程(尤其是自定义Pressable组件是否正确响应键盘事件,RN-Web的Pressable默认键盘可达性并不保证)。

---

## 备注

- 以上6项都还没有对应代码,这份文件只是排期计划。
- 优先级顺序是我的建议,PM可以完全打乱重排。
- 每一项开工前,仍然按`TESTING.md`§4的纪律来(不固定等待、不弱化断言、新测试要在真实环境跑3次绿)。
