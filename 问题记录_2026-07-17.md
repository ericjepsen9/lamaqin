# 问题记录(2026-07-17 · PM离线期间持续更新)

> PM晚间安排:"这段时间安排测试,尽可能多测试几轮。问题都记录,包括以前的问题。我有时间集中处理。"
> 本文件是持续更新的问题清单,不是一次性报告——晚间每发现新问题会追加进来,PM醒来后一次性看。

## 状态图例
- 🔴 未处理,需要PM决策/资源(我这边做不了)
- 🟡 进行中/部分处理
- 🟢 已处理(本次会话)
- ⚪ 更早遗留,与本轮测试无关

---

## 一、需要PM决策或资源、我这边做不了的

1. 🔴 **考试分数DB约束(`exam_grades_score_integer_check`)还没应用到sss-dev**——迁移文件已写(`supabase/migrations/20260717000000_exam_score_integer_check.sql`),但这个会话没有Supabase MCP/任何凭据能连sss-dev,前端校验已经生效(改动会立即随代码生效),DB约束这道防线需要有权限的人/会话去跑。同批还有`20260717000100_quiz_text_length_check.sql`(思考题长度上限)也是同样情况。
2. 🔴 **`expo lint`当前有90个预先存在的问题(59 error/31 warning)**——跟这次改动无关(逐一用`git stash`核实过),几乎全是`react-hooks/set-state-in-effect`(新版eslint规则,可能是依赖升级带来的),分布在十几个文件。量不小,不在这轮任务范围内,如实记录。
3. 🔴 **Android打包**——你之前提到要试Expo后台"Build from GitHub"这条路(不需要CLI/EXPO_TOKEN),不确定后续有没有实际点过、成不成功。
4. ⚪ **老学员内加行迁移处理**(延后-24,等你提供数据)——更早遗留,非本轮新增。
5. ⚪ **设置页推送开关**(决策188·方案A全套,等你排期)——更早遗留,非本轮新增。

## 二、本次会话已处理(供核对,非待办)

- 系统性e2e测试补充第1-13批(83条)+ CI真机跑发现的11+若干处bug,已全部修复,当前分支e2e套件确认过全绿(96条测试,含新增的①②两个维度)。
- 测试计划①异常输入扩展(5个文件,11条新测试)、②权限边界写层面(4个场景,e2e+integration共14条新测试)——已完成,详见commit历史。
- PM决定并已实现:考试分数禁止小数(前端+DB约束文件)、思考题内容加5000字符上限(占位数字,不是教务定的口径,**这个数字本身也可能需要你或教务之后再核实**)、颂词组句/续接排序题门槛从≥1词块提到≥2。
- 思考题管理页(`quiz/[questionId].tsx`)编辑题干/编辑选项/删除题目/保存参考答案,4处mutation此前都没有失败提示(onError),已全部补齐,和全项目其它页面做法保持一致。
- ~~"调整共修日程"/"设休息周"两条CI间歇性90秒超时——诊断到`/classes/[id]`路由此前从未被预热过,补加预热后一轮干净全绿"~~ **这个结论错了,已在下面"晚间持续测试记录"里修正**:补预热那一轮之后,"调整共修日程"又真机复现了同一个90秒超时,证明预热不是(唯一)根因。真根因是`Promise.all([waitForEvent('dialog'), click()])`这个写法本身不安全,已改用验证过的`page.once`+`expect.poll`模式,详见下方晚间记录。

## 三、调研中发现、需要PM知晓的文档/现状偏差(非bug,是信息差)

- 你之前测试计划文档写"报数弹层'今日'计数",实际代码里报数弹层是**本周**口径,真正的"今日"计数是另一个独立组件("今日在修")。晚间写时区测试会按实际代码测,不按文档字面意思测,这里报备一下避免你之后核对时觉得对不上。
- 藏历日历"功德日🌺"这个标记,当前对着种子/共享库数据**恒定不显示**——不是本轮发现的bug,是`lib/tibetan-map.ts`里早就写死的保守设计(数据源判定规则不明确,人工核对命中率只有40-65%,干脆写死false)。测时区边界这条时,这个标记测不到,只能测"佛事文案"和"今日跨天切换"这两块。
- `practice-config/index.tsx`里"适用专业"字段的UI提示文案写"可多选,空=不限",但写④场景2测试时发现:管理端列表实际是按`(t.appliesToPrograms ?? []).includes(选中的专业id)`过滤的——留空(NULL)会变成空数组,空数组`.includes(任何id)`恒为false,等于**这条模板不会出现在"按专业筛选"的任何一个列表里**,跟文案说的"不限"字面意思不一致。没有深入调研这个"不限"具体在哪些地方真正生效(可能是另一个场景,比如学员端自选经候选清单那边的语义,不是这个管理端列表),这里只如实报备"文案与这一处代码行为不一致"这个疑点,不下结论说这是bug。

---

## 四、晚间持续测试记录(以下随进展追加)

### 01:20 · admin-quiz.spec.ts"新建问答题"第二次出现同类间歇性失败
真机CI再次出现(此前在本轮会话更早时候出现过一次,当时只当单次样本没深究):UI显示"创建成功"文案,但立即查DB找不到刚建的那行。跟"调整共修日程"不是同一种报错现象(那个是dialog超时,这个是DB查不到行),但怀疑是同一类根因——`/quiz/new`这条路由也是全套件里只有admin-quiz.spec.ts会碰、也从来没被预热过。已经顺手把这类"静态、不需要动态ID"的admin路由一次性多预热了几条(`/dashboard` `/quiz/new` `/practice-config` `/scheduling` `/students` `/reminder-presets` `/events`),降低今晚后续再撞见同类问题的概率,不是逐个反应式地修。**这条具体的因果关系没有100%验证**,今晚会持续观察是否复现。

### 🔴→🟢 PM报告"登录无限循环卡在重设密码页面,不能返回、跳过"(真实产品bug,非测试噪音)
**根因已confirm、已修复**:`app/(auth)/set-password.tsx`的`forced=1`(强制改密码,老学员默认密码场景)提交成功后,只把`profiles.must_change_password`写回数据库,没有同步更新`useCurrentUser()`的前端查询缓存。`proceed()`跳回入口闸门(`app/index.tsx`)时,闸门这一次渲染读到的还是缓存里的旧值(`true`),立刻把人重新弹回`/set-password?forced=1`——而forced模式设计上又没有"跳过"按钮(否则默认密码没法强制改),也没有"返回"按钮(整个AuthLayout就没header),所以是真死循环,不是网络抖动。

已核实排除的其它可能:①`profiles_protect_status`触发器不保护`must_change_password`这一列,不是触发器把值悄悄revert回去 ②RLS允许本人自己改这一列,不是权限被拒。纯粹是前端缓存没跟着写库的结果同步。

修复(3处):
1. `profiles`更新加了错误检查(此前完全没检查这一步是否失败,失败了也会静默继续跳转)。
2. 成功后直接patch查询缓存(不等后台重新拉取那一轮网络请求,当场同步)。
3. forced模式加一条"退出登录"逃生口(仿`pending.tsx`/`account-deletion-pending.tsx`的既有写法)——万一改密码这步本身出错(网络/服务端),至少还有路可退,不会被真困死。

新增e2e回归测试(`e2e/set-password-forced.spec.ts`):模拟一个`must_change_password=true`的学员登录→落在强制改密码页→提交新密码→断言真正放行到`/home`(不是被弹回原页)→**额外做一次整页硬刷新**独立验证数据库层确实写回了`false`(不只是前端缓存"看起来"过关)。此前这条路径完全零覆盖(唯一相关测试只断言"创建时这个标记被正确置true",从没测过"学员真的登录走完这一页"这一步)。

**状态**:已修复+已写回归测试+本地tsc/eslint过,已push,CI结果待确认(本环境无本地Docker/Supabase栈,只能等CI跑)。

### 补记·核对之前两轮CI时发现:"调整共修日程"预热修复的结论错了 + 新增时区测试自身2处bug
修login bug时顺手核对了之前两次push(`69f4982`预热修复、`bf2a34f`新增时区测试)真机CI的结果,发现:

1. **"调整共修日程"真机CI又复现了同一个90秒超时**(`69f4982`那一轮),即使`/classes/[id]`已经预热——证明"路由没预热"这个此前的结论是错的,或至少不是唯一根因。回头看这条测试用的`Promise.all([waitForEvent('dialog'), click()])`,跟本会话早前在"任命为爱心""从讲记提取""course-self-study重复点击"这几处已经确认过、也修过的同一类真实bug(click()触发的原生alert会阻塞渲染进程,顺序await导致click()本身挂起)是一模一样的写法——2026-07-16曾怀疑过这条也是同一类问题,当时诊断两轮都很快就下结论"CI偶发抖动",这次复现证明那个结论错了。已改用同一套`page.once`+`expect.poll`稳妥模式修复;"设休息周"的添加步骤是同一种不安全写法(此前两轮侥幸没复现),一并预防性修了,不等它也真的坏一次才动手。
2. **新增的`timezone-boundary.spec.ts`两条测试真机CI首跑双双失败**(`bf2a34f`那一轮),但两处都是**测试自己写错了,不是产品bug**:①清理顺序错了,先删`programs`、后删还在引用它的`cohorts`,撞`cohorts_program_id_fkey`(这个FK是`ON DELETE RESTRICT`)②日历页同一个节日名会在页面好几处DOM节点重复渲染,`getByText`没加`.first()`撞Playwright严格模式(resolved to 3 elements)。两处都已修,还没等到这两条测试自己的下一轮CI确认。

**如实说明**:这两轮CI其实早就跑完并报了失败,但当时(晚间自主测试阶段)因为专注在写下一批新测试、没有回头逐条核对已push commit的CI真实结果,把"push了看起来合理的修复"和"CI真的验证通过"混为一谈——这次是趁着查login bug顺手回头核对才发现的。以后每次push后应该实际等/查一次CI结果再往下走,不能假设"逻辑上应该对"就等于"测试真的绿了"。

### 登录死循环修复——CI已实测确认,不是只有代码推理
`b4682da`(登录修复+回归测试`set-password-forced.spec.ts`)那一轮CI跑完了:新写的回归测试**真的通过**("✓ ... 真正放行、不再被弹回 (5.2s)"),那一轮唯一的2处失败是当时还没来得及修的timezone-boundary.spec.ts那2个测试自身bug(在后续commit `98915c8`里已经修了)——不是登录修复引入的新问题。也就是说登录死循环这个bug,现在不只是"我看代码觉得应该对了",是CI真机跑过一次、confirm了修复前会死循环卡住的场景现在能正常走完。

### 又追加做的两件事(顺着"继续测试"的指示往下推)
1. **系统性排查全项目`Promise.all([waitForEvent('dialog'), click()])`这个已确认不安全的写法**:除了"调整共修日程"那一处,全项目还有8处用同一种写法(admin-attendance.spec.ts 4处、admin-scheduling.spec.ts 2处、admin-quiz.spec.ts 1处、admin-class-management.spec.ts 1处),目前都还没实测复现过,但既然根因已经confirm,不等它们各自哪天真的抽到那个时序才逐个修,一次性全部换成验证过的稳妥模式。
2. **测试计划④并发/竞态,2个场景都写完了**:①两个真实浏览器上下文同时对同一学员提交出勤(不同状态)——不假设谁赢谁输,只钉住"最终必须恰好1行"这个数据完整性底线,如实记录两边各自收到的结果②确认功课模板"整行UPDATE、无字段级合并"这个已知设计现状——admin甲编辑框开着时,乙抢先改的字段会被甲提交时悄悄覆盖回旧值,这不是bug,是现有设计的真实行为,让你知道有这么回事。

都已经push(`6770e7a`+`1f28e4c`),CI还在跑,结果待确认。

### 跟进这两轮CI的真实结果:2处新测试自身bug,1处三度复现的老问题
`6770e7a`(dialog系统性排查)和`1f28e4c`(④并发测试)这两轮CI都跑完了,各有1处失败,查了真实log(不是猜):

1. 🟢 **`1f28e4c`失败**:我新写的④场景2测试自己漏了步骤——`practice-config`这页的模板列表是按"当前选中的专业chip"过滤的,不选专业/模板没挂`applies_to_programs`,列表就是空的,断言等不到卡片出现。参照"编辑模板"那条已有测试的写法补上专业+选中chip这两步,已修。
2. 🔴→🟡 **`6770e7a`失败,但不是这次改动引入的新问题**:是`admin-quiz.spec.ts`"客观题payload编辑器·颂词续接"这条测试,报错是`expect(created.length).toBe(1)`收到`0`——UI已经显示创建成功(断言通过),但紧接着立刻查DB这一行还查不到。**这是同一个症状第3次在"新建思考题"这条创建链路上出现**(前两次分别在"新建问答题"测试上出现过、当时都当单次样本没深究)。

   这次认真查了代码(`useCreateQuestion`的INSERT、`useAdminQuestion`详情页的SELECT),两处都是走真实的Supabase INSERT/SELECT,没找到"乐观缓存""伪造成功"这类能100%解释这个现象的应用层bug。但3次现象一致:都是"查一次没有",不是"永久丢了"(如果真丢了,大概率会在后续多个断言里持续失败,不会只在这一步)。**如实说明:没能100%查清楚根因**(可能是CI runner资源紧张时数据库可见性偶尔慢半拍这类环境因素,但没法在这个会话里实测验证)——按现有代码里`admin-scheduling.spec.ts`等好几处"刚提交、要等DB追上"场景的既有写法,把这两处单次查询改成`expect.poll`重试(不是放宽断言标准,是承认这个环节可以短暂重试)。这样处理还有个好处:如果哪天再复现,能分清是"重试一下就有了"还是"重试很多次还是没有"这两种性质完全不同的情况。已推送,还没等到这两条改过的测试自己的下一轮CI确认。
