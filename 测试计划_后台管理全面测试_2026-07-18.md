# 测试计划·后台管理全面测试(2026-07-18)

> **状态:方向已定稿(2026-07-18 你确认了 §4 的 4 个问题),按 §3 顺序开工。**
> 范围:`app/(admin)/**` 全部 16 个功能区。你这次要求 4 个维度:①UI 交互便利性 ②按钮点击功能可用性 ③功能正确性 ④页面跳转正确性。
> 与既有测试的关系:`测试计划_下一步_2026-07-16.md`(①异常输入②权限边界③时区边界④并发竞态⑤数据量规模⑥键盘可达性)已执行完毕(任务 #68/69/73/74 等),那批聚焦"数据/业务语义正确性"这条线,且大多是**已有页面的深化**。这次你要的 4 个维度是不同关注点——更偏 UI/交互/导航层,而且会**第一次系统性覆盖此前完全零测试的几个后台页面**。
> **本版更新**:在第一版路由级盘点基础上,追加了逐页/逐控件的代码走查(哪些按钮真的接了线、接的是什么),把批次从"测这个功能区"细化到"测这几个具体按钮/组件",批次优先级也按实际走查结果重排过。

---

## 0 · 现状盘点(路由级,已验证——逐条核对 `e2e/*.spec.ts` 里所有 `page.goto()` 实际访问过的路由)

| # | 功能区 | 路由 | 可见角色 | 现有 e2e 覆盖 | 本次 4 维度缺口 |
|---|---|---|---|---|---|
| 1 | 总览 Dashboard | `/dashboard` | admin/zhumai/aixin | 仅测过"学员深链被弹回",**内容零测** | ①②③④全缺(见 §1) |
| 2 | 学员管理 | `/students`、`/students/[id]` | admin/zhumai/aixin | 列表页(宽屏用嵌入面板)测过审批/拒绝;**独立路由 `/students/[id]` 从未 goto** | 详情页①②③④全缺(见 §1,最大缺口) |
| 3 | 班级管理 | `/classes`、`/classes/[id]` | admin/zhumai/aixin | 较全 | ①待细化 |
| 4 | 共修与讲考 | `/attendance`、`/attendance/[id]`、`/attendance/speaking/[id]` | admin/zhumai/aixin | 较全 | ①待细化 |
| 5 | 关怀清单 | `/care`、`/care/[studentId]` | admin/zhumai/aixin | 详情页测过"加跟进";**列表页 `/care` 从未 goto** | 列表页①②③④全缺(见 §1) |
| 6 | 报数升学 | `/advancement`、… | admin/zhumai | 较全 | ①待细化 |
| 7 | 课程内容 | `/course-content`、`[courseId]`、`book/[bookId]`、`lesson/[lessonId]` | **admin only** | 零覆盖 | **修正**:2026-07-11 PM 已裁定 App 端内容维持只读(编辑归官网/ETL 线),这里没有 CRUD 好测——真正的测试目标是导航链路(列表→课程详情→lesson详情/文章)走得通、不崩溃,工作量比我第一版估的小很多 |
| 8 | 排课管理 | `/scheduling` | admin only | 较全 | ①待细化 |
| 9 | 功课配置 | `/practice-config` | admin only | 较全 | ①待细化 |
| 10 | 自学管理 | `/selfstudy-manage` | admin only | 零覆盖 | 只有 1 处真 mutation(节奏 Stepper),其余是 v1 范围内故意只读的名单 |
| 11 | 题库 | `/quiz`、… | admin only | 较全 | ①待细化 |
| 12 | 法会管理 | `/events` | admin/zhumai | 新建/加场次/发公告测过;**配功课 / 启用停用 / 删场次 3 处零测** | 见 §1,具体易补 |
| 13 | 藏历画报 | `/poster-calendar` | admin only | 仅本次强调色预览测试;**上传图片/保存月度画报/法会期&特别日 CRUD 全未测** | 大部分①②③④缺 |
| 14 | 提醒语库 | `/reminder-presets` | admin/zhumai/aixin | 仅测"双击去重";**编辑 / 停用启用 2 处零测** | 见 §1,具体易补 |
| 15 | 反馈清单 | `/feedback` | admin only | 零覆盖 | ①②③④全缺 |
| 16 | 系统审计 | `/audit` | admin only | 仅测过"zhumai 被弹回",**内容零测** | ①②③④全缺(见 §1) |

---

## 1 · 逐控件走查结果(新增,取代"猜测哪里有缺口"——每条都对应真实代码里的一个按钮/组件)

> 说明:下面只列**有真实 mutation 或导航、但零测试覆盖**的控件;纯展示/只读元素不列。每条标了对应的 hook/组件名,方便日后直接照着写测试。

### `app/(admin)/students/[id].tsx` — 学员管理详情页(遗漏面最大)
**重要前提**:Playwright 默认视口(1280px,≥900 算"宽屏")下,`/students` 用**主从布局**,点学员是在同一页嵌入一个侧栏面板(复用同一批组件),**不会跳转到 `/students/[id]` 这个独立路由**——只有窄屏/移动视口才会真正走到这个路由。现有的"批准/拒绝"测试是经**宽屏嵌入面板**测的,**独立路由本身从未被访问过**。
**已定(2026-07-18):两条路径都测**——宽屏嵌入面板 + 显式设窄视口触发的独立路由,都要覆盖。

零覆盖的具体控件(共约 15 个,按角色分 3 层):
- **admin 专属**:自学资格 授予/撤销(`grantSS`/`revokeSS`)、传承记录 录入(`StudentTransmissions`)、无障碍学修 视力/听力障碍 chip 切换(`updateA11y`)、"设宽限/暂停/纠错"弹层里的座次门槛/每日目标覆盖(`updateMinSession`/`updateDailyTarget`)、切主班(`switchPrimary`)、代行记录(`recordProxy`,类型/对象/替代修法/理由全套)
- **zhumai/aixin 专属(admin 看不到这个按钮,只读)**:关怀日志 添加记录(`addFollowup`,与 `/care/[studentId]` 共用组件,组件本身测过但从没在这个路由下测过)
- **三角色都能看到但零测**:旁听转正(`promoteButton`,已有 testID)、暂停/恢复某条愿(`pauseVow`/`resumeVow`)

### `app/(admin)/events/index.tsx` — 法会管理剩余 3 处(具体易补)
- **配功课**(`EventPracticeModal`,admin 专属):选修法 chip + 可选目标数 → `useSetEventPractice`。零测试,无 testID。
- **启用/停用法会**(admin 专属,停用需二次确认):`useToggleEventActive`。零测试,无 testID。
- **删场次**(`SessionsModal` 内"删"按钮,admin 专属,需二次确认):`useDeleteEventSession`。零测试(只测过"加场次"),无 testID。

### `app/(admin)/reminder-presets/index.tsx` — 提醒语库剩余 2 处(具体易补)
- **编辑预设**:复用新建同一个弹层(`isEdit=true`)→ `useUpdateReminderPreset`。零测试,且编辑态和新建态共用同一批 testID,需要先补区分。
- **停用/启用切换**:`useToggleReminderPresetActive`。零测试,无 testID。

### `app/(admin)/dashboard.tsx` — 总览内容(目前只测过"进不去",没测过"进去了看到什么")
4 个 KPI 卡各自导航(待审批→`/students`、在读→`/students`、共修出勤→`/attendance`、需关怀→`/care`)、审批队列"批准/拒绝"、关怀提醒"查看"、2 处"查看全部→"——共 8+ 个已接线控件,一个都没在真实 admin 会话下验证过内容。

### `app/(admin)/audit/index.tsx` — 系统审计内容(同上,只测过"进不去")
筛选 chip、搜索框(仅搜已加载的行,2026-07-11 修过这个范围问题)、"加载更多历史记录"分页按钮——一个都没在真实 admin 会话下验证过。

### `app/(admin)/care/index.tsx` — 关怀清单列表页
筛选(全部/需关怀/高滞后)、搜索(姓名/班级)、点学员卡片进详情——纯导航+本地筛选,零测试。

### `app/(admin)/feedback/index.tsx` — 反馈清单
筛选 chip(全部/待处理/内容纠错/问题/建议/其他)、按当前状态动态变化的状态流转按钮(标记处理中/已解决/关闭/重新打开)→ `useUpdateFeedbackStatus` 一类的 mutation。零测试。

### `app/(admin)/selfstudy-manage/index.tsx` — 自学管理
唯一的真 mutation:每个专业的默认周节奏 +/- Stepper(1-21 钳制)→ 写 `programs.default_weekly_lessons`。零测试。其余学员名单是 v1 范围内故意只读,不需要测 CRUD。

### `app/(admin)/course-content/**` — 课程内容(4 个文件,已修正为"只读,轻量测")
2026-07-11 决策已明确 App 端内容维持只读,之前的编辑入口已被删除(不是遗漏,是决策后的死代码清理)。唯一需要测的是导航链路本身走得通:列表(班级课程/自学读物两个 tab)→ 课程详情→lesson 详情;`book/[bookId].tsx` 里的"查看"按钮 2026-07-10 修过一次死按钮(当时点了没反应),这条建议直接补一条回归锁定测试。

### `app/(admin)/poster-calendar/index.tsx` — 藏历画报(我自己刚做完主题色功能,顺带盘点)
除了这次新加的强调色预览测试,月度画报的"上传图片/粘贴链接保存/启用停用"、法会期&特别日画报的"新增/编辑/删除/日期重叠提示"全部零测试。

### `app/(admin)/index.tsx`
纯 `<Redirect>` 桩,全仓没有任何地方会导航到这个裸路径(真实入口 `app/index.tsx` 直接判断角色后跳 `/dashboard`)。**建议跳过**,不是用户可达路径,测了也没有实际意义。

---

## 2 · 4 个维度分别怎么落地

### ① UI 交互便利性
现状:全仓没有系统化方法,历史上是"你真机踩到 → 报告 → 我修"的被动模式(如 #81 弹窗键盘遮挡、这次的日期时间选择器改造)。这次建议转成**主动系统性走查**,逐页套统一 checklist,不再等报告。

**通用便利性 checklist(逐页套用,能写成断言的写断言,主观项标"需人工看")**:
- [ ] 加载态:数据未到达前有 loading 指示,不是白屏/闪一下空态
- [ ] 空状态:列表为空有 EmptyState 文案,不是空白
- [ ] 错误态:查询/提交失败有可读中文提示,不是静默或原始英文报错
- [ ] 表单校验:必填未填/格式错误,按钮 disable 且有文案说明原因
- [ ] 弹窗+键盘:含输入框的弹窗包了 `KeyboardAvoidingView`(#81 修过大部分,新页面需复核)
- [ ] 弹窗误关:含未保存输入的弹窗,点遮罩不会误关丢数据
- [ ] 危险操作确认:删除/终止/不可逆操作有二次确认(`confirmAsync`)
- [ ] 重复提交防抖:关键写操作点两次只生效一次
- [ ] 长列表性能:数据量大不卡顿
- [ ] 文字/色彩:有没有文字截断看不全、对比度不足
- 色彩/留白协调度这类纯主观项:按你 2026-07-18 的决定,不额外交截图集,我按现有色值规范(`lib/theme.ts`)和既有页面风格自行判断即可。

### ② 按钮点击功能可用性(死按钮审计)
上面 §1 的逐控件走查**已经把这一步做了大半**:每一条都标了"是否真的接了 mutation/导航"。走查结果:目前找到的所有控件**都是真接线的**(没有发现空函数/占位 TODO 这类死按钮),缺口在于"接了线但没人点过一次去验证它真的работает"——所以②的落地方式收窄成:上面 §1 列的每一条,补至少一条 e2e 断言"点击后发生了预期的 DOM 变化或 DB 写入"。

### ③ 功能正确性
沿用 §1 的逐控件清单即可,每条都对应一个可断言的最终状态(mutation 成功后的 DB 行/字段)。

### ④ 页面跳转正确性
1. **角色矩阵回归**(性价比最高):16 个功能区 ×4 种角色的可见性矩阵写死在 `NAV_ITEMS`,但只验证过 2 个反面 case(不该看的被弹回),**没有一个正面 case**(该看的能不能正常进去)。建议每个功能区都补双向验证。
2. **站内导航**:列表→详情→返回的路径对不对;`/students` 的宽屏嵌入面板 vs 窄屏独立路由,两条路径都要过一遍(见 §1 开头那条重要前提)。
3. **深链参数正确性**:点击哪一行,进去的详情是不是对应那一行(如 `/attendance/[id]`、`/quiz/[questionId]`)。

---

## 3 · 批次建议(已定稿,按此顺序开工)

**批①·最大遗漏面**:`/students/[id]` 全部 15 个控件(宽屏嵌入面板 + 窄屏独立路由两条路径都测,见 §1)
**批②·具体易补**:法会管理配功课/启用停用/删场次(3 处,admin 专属)
**批③·具体易补**:提醒语库编辑/停用启用(2 处)
**批④·补"真内容"测试,替换现在"只测拒绝"的假覆盖**:Dashboard 总览 8+ 控件 + 系统审计筛选/搜索/分页
**批⑤·列表+轻量 CRUD**:关怀清单列表 + 反馈清单状态流转 + 自学管理 Stepper(唯一真 mutation)
**批⑥·画报 CRUD 补完**:月度画报上传保存 + 法会期/特别日画报增删改
**批⑦·只读区域,轻量验证**:课程内容导航链路(含 2026-07-10 那个死按钮的回归锁定)
**批⑧·角色矩阵批量回归**:16 个功能区 × 角色矩阵双向验证(写好 1 个可复用 helper)

①便利性 checklist、②死按钮审计穿插在每批里做,不单独占批次。

---

## 4 · 定夺记录(2026-07-18)

1. `/students/[id]` 视口:**两条路径都测**(宽屏嵌入面板 + 窄屏独立路由)。
2. UI 便利性主观项:**不额外交截图集**,我按现有色值规范自行判断。
3. §3 批次顺序:**按现在这个顺序开工**,不调整。

---

## 附:与 `测试计划_下一步_2026-07-16.md` 的关系

那份计划的 ①-⑥ 已执行完毕,可视为收口,不需要重做。
